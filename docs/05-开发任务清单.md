# 知识库开发任务清单

> 执行原则：按阶段完成；每项任务只有在代码、测试和文档同时完成后才能勾选。当前工作摘要、验证记录和阻塞项只维护在 [TASK.md](../TASK.md)。
> 架构：TypeScript/NestJS 主服务 + Python/FastAPI 文档解析 Worker。

---

## 0. 完成定义

每个任务的 Definition of Done：

- 实现符合 [01-项目实施规格.md](./01-项目实施规格.md)。
- 关键设计符合 [02-技术设计.md](./02-技术设计.md)。
- 包含成功、失败和权限相关测试。
- lint、类型检查、单元测试通过。
- 不包含真实 API Key、用户数据或本机绝对路径。
- `.env.example`、README 或相关文档同步更新。
- 日志不泄露密钥和完整敏感正文。

---

## 阶段 16 前遗留与明确延期

以下未完成项目原属于阶段 4 或阶段 7，但不在当前已交付白名单或运行时 Provider 范围内。它们不阻塞进入阶段 16，也不得被描述为已经实现；若阶段 16 的压测、恢复演练或已批准的业务需求证明其成为生产上线前置条件，必须重新提高优先级并同步 [TASK.md](../TASK.md)。

### 计划增强

- [ ] PPTX 解析与预览（原阶段 4.4）。
- [ ] HTML 解析与预览（原阶段 4.4）。
- [ ] RTF/EML 解析与预览（原阶段 4.4）。
- [ ] OpenAI Embedding 的运行时配置、Factory 接入和契约测试（原阶段 7.2）。

---

已完成阶段默认折叠；阶段 16 前遗留与明确延期、阶段 14 的评测结论和阶段 16 保持展开。完整的当前工作摘要、验证记录和阻塞项见 [TASK.md](../TASK.md)。

<details>
<summary>已完成阶段 1–13（遗留与延期项已移至上方）</summary>

## 阶段 1：仓库与开发环境

### 1.1 Monorepo

- [x] 初始化 Git 仓库和 TypeScript workspace。
- [x] 创建 `apps/api` NestJS 主服务。
- [x] 创建 `apps/parser-worker` Python FastAPI 服务。
- [x] 创建 `packages/contracts`。
- [x] 开启 TypeScript strict mode。
- [x] 配置 ESLint、Prettier 和测试框架。
- [x] 配置 Python formatter、lint、type check 和 pytest。
- [x] 添加 `.gitignore`。
- [x] 添加 `.env.example`。
- [x] 添加根目录 `README.md` 和 `AGENTS.md`。
- [x] README 提供新手基础模式、环境准备、分步骤本地启动、按需高级配置和常见问题 FAQ。
- [x] 为 `apps/`、NestJS API、Vue Web 和 Parser Worker 提供目录级 README；解析器说明覆盖当前算法、限制和安全边界。
- [x] 以 `docs/README.md` 统一文档导航与事实源，`pnpm docs:check` 检查格式、本地链接/锚点、API 端点、Rerank Provider 和视口断点，并纳入默认 lint/CI。

验收：

- [x] TypeScript 与 Python 的安装、检查和测试命令均能运行。
- [x] 仓库中没有 `.env`、密钥、上传文件和向量数据。

### 1.2 本地基础服务

- [x] 编写主服务 Dockerfile。
- [x] 编写 Python Worker Dockerfile；PyPI 依赖解析和 EasyOCR 离线模型预下载具备有限重试，模型下载与依赖层分离以保留构建缓存。
- [x] Compose 加入 Redis。
- [x] Compose 加入 Chroma Server。
- [x] 预留 PostgreSQL 服务。
- [x] 配置共享只读/读写文档 volume。
- [x] 为所有服务增加 healthcheck。
- [x] 容器使用非 root 用户。
- [x] 提供仅绑定 `127.0.0.1` 的 PostgreSQL/DBeaver 本地调试覆盖文件，生产默认不加载。
- [x] 本地文档明确 Docker Desktop 启动前置条件，以及 `.env` 按服务重建、镜像重建与数据库初始化配置的区别。

验收：

- [x] `pnpm docker:up:dev` 可启动推荐本地开发服务；具备 ODA 前置条件时，`pnpm docker:up:all` 可额外启动 DWG Worker 与全部常驻 Docker 服务。
- [x] 主服务只能通过内部地址访问 Worker、Redis 和 Chroma。
- [x] 停止再启动后 Chroma 数据仍存在。
- [x] PostgreSQL 默认不暴露宿主机端口；显式加载本地调试覆盖文件时只监听回环地址。

---

## 阶段 2：配置、日志与基础契约

### 2.1 配置

- [x] 使用 schema 校验本阶段全部环境变量。
- [x] 将根目录 `.env.example` 设为唯一完整配置清单，并为每个配置项提供紧邻的用途说明；其他文档只保留链接或场景化最小片段。
- [x] 分离 local、test、production 配置。
- [x] 验证所选 Embedding Provider 的 Key、base URL 和 model。
- [x] 启动时拒绝缺失或不合法配置。
- [x] 输出配置摘要时对 Key 完全脱敏。

### 2.2 日志与错误

- [x] 配置 Pino JSON 日志。
- [x] 每个 HTTP 请求生成/透传 trace ID。
- [x] 定义统一 API 错误结构。
- [x] 定义 Embedding Provider 错误类别。
- [x] 配置全局异常过滤器。
- [x] Python Worker 使用相同 trace ID。
- [x] 日志字段加入 tenant、job、document 和 provider 标识。

### 2.3 跨服务契约

- [x] 定义 ParseRequest、ParsedElement、ParseResponse。
- [x] 生成或手工维护 JSON Schema/OpenAPI 契约。
- [x] 新增公开 API 使用说明，并以 OpenAPI 为字段事实源覆盖认证、权限、端点、错误码、分页和安全示例。
- [x] 主服务对 Worker 响应做运行时验证。
- [x] Worker 校验内部 token。
- [x] 契约测试覆盖缺字段、错误类型和超大响应。

验收：

- [x] 主服务和 Worker 能完成一条内部 ping/parse mock 调用。
- [x] 错误响应不包含堆栈、内部路径和密钥。

---

## 阶段 3：文件上传与文档管理

### 3.1 上传安全

- [x] 实现流式上传。
- [x] 限制文件大小。
- [x] 校验扩展名、MIME 和 magic bytes。
- [x] 使用 UUID 存储文件名。
- [x] 规范化并单独保存原始文件名。
- [x] 计算 SHA-256。
- [x] 拒绝路径穿越和符号链接逃逸。
- [x] 上传文件不包含执行位。

### 3.2 文档记录

- [x] 定义 Document、DocumentVersion、IngestionJob 数据模型。
- [x] 实现文档创建和状态查询。
- [x] 实现内容哈希去重策略。
- [x] 实现软删除状态。
- [x] 实现按 tenant 查询。

### 3.3 API

- [x] `POST /v1/documents`。
- [x] `GET /v1/documents/:id`。
- [x] `DELETE /v1/documents/:id`。
- [x] `GET /v1/ingestion-jobs/:id`。

验收：

- [x] 上传返回 documentId 和 jobId，不同步等待解析。
- [x] 两个 tenant 无法读取彼此文档状态。
- [x] 超大、伪造 MIME 和非法扩展名被拒绝。

---

## 阶段 4：Python 解析 Worker

### 4.1 安全与框架

- [x] 实现内部 `/internal/v1/parse`。
- [x] 校验内部 token。
- [x] 只允许访问指定共享目录。
- [x] 拒绝 `..`、软链接逃逸和任意绝对路径。
- [x] 配置文件大小、解析时间和资源限制。
- [x] 增加 live/ready 健康检查。

### 4.2 第一阶段解析器

- [x] TXT 解析器。
- [x] Markdown 解析器。
- [x] PDF Unstructured 解析器（文本 PDF 与扫描 PDF 本地策略、页数和加密限制）。
- [x] DOCX 解析器。
- [x] DOCX 解析器。
- [x] DOC/Tika 解析器与 LibreOffice PDF 预览。
- [x] XLSX/openpyxl 解析器。
- [x] PNG/JPG/JPEG EasyOCR 解析器（CPU、离线模型、像素限制）。
- [x] Tika 兜底策略：仅在 PDF 的 Unstructured 非安全校验失败或空结果时使用固定内网服务，含超时、返回体上限、readiness 与 fallback warning。

### 4.3 结构化输出

- [x] 输出 elementType。
- [x] 输出页码或工作表。
- [x] 输出标题路径。
- [x] 表格保留列名和上下文。
- [x] OCR 输出置信度、bbox 和低置信度数量 warning。
- [x] 空解析结果返回失败。
- [x] 返回 parser 与 parserVersion。
- [x] Parser Worker CI 在 step 运行期初始化 runner 临时模型目录，真实 PDF/OCR fixture 覆盖 Ruff、Mypy 与 pytest；Pydantic 内部构造使用 snake_case 字段名，对外 JSON 保持 camelCase alias；预览资源异常分类使用 Python 3.11 联合类型语法并通过 Ruff `UP038` 检查。

### 4.4 第二阶段解析器当前交付

PPTX、HTML、RTF 和 EML 已移至“阶段 16 前遗留与明确延期”，不计入本折叠区的完成状态；DOC 已完成并纳入当前白名单。

- [x] DXF/ezdxf。
- [x] DWG 上传签名校验、受控 ODA 转 DXF 和 ezdxf 解析。
- [x] DWG `converting` 任务状态与前端不确定进度条规范。
- [x] CAD 复杂解析与渐进预览的块展开、颜色、边界、主体聚焦、瓦片缩放、隐藏图元可见性和资源预算收口；具体实现与回归记录以 Git 历史为准。

验收：

- [x] 每种第一阶段格式至少有一个固定测试样本；PDF 和 PNG fixture 在测试启动时确定性生成，并通过真实 Unstructured/EasyOCR 路径验证。
- [x] 当前已支持格式的解析结果包含标题路径或工作表信息。
- [x] Worker 无模型 Key、无 Chroma 权限、无公网入口。

---

## 阶段 5：异步入库编排

### 5.1 BullMQ

- [x] 创建 ingestion queue 和 worker。
- [x] 使用 ingestionJobId 作为队列 jobId。
- [x] 定义状态机和 checkpoint。
- [x] 按错误类型细分重试。
- [x] 配置 dead-letter/failed job 查询。
- [x] 配置优雅停机。

### 5.2 Worker 调用

- [x] 主服务发送文件引用，不通过 Redis 传大文件。
- [x] 设置连接与总超时。
- [x] 透传 trace ID。
- [x] 校验 Worker JSON 响应。
- [x] 保存解析器版本和 warning。
- [x] CAD/转换/解析容量错误支持管理员复用原文件人工重排；队列仍不对容量错误自动重试。

验收：

- [x] 重复消费同一任务不会创建重复文档版本。
- [x] Worker 暂时不可用时任务按策略重试。
- [x] 主服务重启后任务状态可恢复。

---

## 阶段 6：分块、脱敏与出网策略

### 6.1 分块

- [x] 按标题、段落、表格和页码优先分块。
- [x] 超长元素按 token 切分。
- [x] 配置 chunk token 上限和 overlap。
- [x] 表格保留表头。
- [x] 相邻 chunk 保存顺序关系。
- [x] 生成稳定 chunkId。

### 6.2 脱敏

- [x] 手机号。
- [x] 身份证。
- [x] 银行卡号。
- [x] 邮箱。
- [x] 可配置业务正则。
- [x] 保存 redaction policy version。
- [x] 日志不输出脱敏前文本。

### 6.3 云端策略

- [x] public/internal/confidential 策略。
- [x] confidential 默认阻止 Embedding。
- [x] 按 provider/区域预留精细放行规则。
- [x] 阻止时产生可审计、无正文的事件。

验收：

- [x] 相同输入生成相同 chunkId。
- [x] confidential 未放行时 Provider mock 调用次数为 0。
- [x] 分块结果仍能还原来源和页码。

---

## 阶段 7：Embedding Provider

### 7.1 抽象

- [x] 定义 EmbeddingProvider 接口。
- [x] 区分 `embedDocuments` 和 `embedQuery`。
- [x] 实现 Provider factory。
- [x] 定义配置指纹。
- [x] 验证返回数量和维度。

### 7.2 平台

- [x] Alibaba Embedding。
- [x] Google `gemini-embedding-001`（原生 SDK、128–3072 维、document/query 任务规则、降维归一化和契约测试）。
- [x] Ollama 本机 Embedding（受控本机端点、无 API Key、配置与响应契约测试）。
- [x] 为 custom provider 预留接口。
- [x] 明确 DeepSeek 不作为 Embedding 默认选项。

OpenAI Embedding 的可选配置与 Factory 接入仍在“阶段 16 前遗留与明确延期”；Google 已进入运行时可选范围。

### 7.3 稳定性与成本

- [x] 按供应商限制批量调用。
- [x] 429、超时和 5xx 指数退避。
- [x] 401/403/400 不重试。
- [x] 记录实际 Provider、模型、输入量、耗时和估算成本；Provider 未返回 usage 或未配置价格时保持未知，不伪造数值。
- [x] tenant + 完整向量配置指纹 + task rule + 脱敏文本哈希缓存复用，带 TTL 与删除清理。
- [x] Embedding 批次缓存和 checkpoint；中间批次失败后重试跳过已缓存 Provider 调用。

验收：

- [x] 文档数与向量数一致。
- [x] 文档和查询向量维度一致。
- [x] 切换模型不会继续写旧 collection。
- [x] Embedding 失败不会自动切换供应商污染索引。

---

## 阶段 8：Chroma VectorStore

- [x] 定义 VectorStore 接口。
- [x] 实现 Chroma TypeScript adapter。
- [x] 实现 collection 配置指纹。
- [x] 使用 cosine 距离。
- [x] 实现稳定 ID upsert。
- [x] 实现 tenant + ACL 查询过滤。
- [x] 实现按文档删除。
- [x] 实现 health check。
- [x] 启动时校验 collection 与 Embedding 配置一致。

验收：

- [x] 重复入库不增加重复 chunk。
- [x] 删除文档后所有版本向量消失。
- [x] 错误维度或模型指纹阻止服务就绪。

---

## 阶段 9：认证和 ACL

### 9.1 本地身份

- [x] 仅开发和测试环境支持固定身份。
- [x] `AUTH_REQUIRED=true` 时禁止开发身份。

### 9.2 生产认证预留

- [x] JWT/OIDC guard 接口。
- [x] Identity 包含 userId、tenantId、department、roles、allowedSensitivities。
- [x] 客户端请求 DTO 不包含可信角色字段。

### 9.3 ACL

- [x] tenant 强制过滤。
- [x] 部门过滤。
- [x] 敏感度过滤。
- [x] 管理员逻辑仍保留 tenant 边界。
- [x] Rerank 前二次检查。
- [x] LLM 前二次检查。
- [x] 引用返回前最终检查。

验收：

- [x] tenant 交叉测试全部失败关闭。
- [x] 用户伪造 role/department 无效。
- [x] 当前文档、任务和 Vector ACL 自动化测试越权率为 0。

---

## 阶段 10：LLM 与 Rerank

### 10.1 LLM Provider

- [x] 定义 LlmProvider 接口。
- [x] OpenAI provider。
- [x] Google provider。
- [x] DeepSeek provider。
- [x] Alibaba provider。
- [x] Custom OpenAI-compatible provider。
- [x] 记录实际 provider/model/requestId/usage。
- [x] 将响应正文读取期间的 `AbortError` 归类为可重试 `timeout`，不误报为不兼容响应；Google 与 OpenAI-compatible adapter 均有回归测试。

### 10.2 提示与输出

- [x] 系统提示限制为基于资料回答。
- [x] 将片段声明为不可信数据而非指令。
- [x] 统一 `[来源N]` 标记。
- [x] 校验来源编号存在。
- [x] 无足够相关结果时不调用 Rerank；混合模式只向 LLM 发送通过出网策略的问题，不发送文档片段，严格模式不调用 LLM。
- [x] 防止文档 prompt injection 触发操作。

### 10.3 Rerank

- [x] 默认 `none`。
- [x] 实现 RerankProvider 接口。
- [x] 至少实现一个云端 provider。
- [x] Rerank 失败降级为向量排序。
- [x] 记录降级事件。
- [x] 本地 `BAAI/bge-reranker-v2-m3` 通过独立内部 Reranker Worker 接入；主服务继续负责 ACL/策略检查，Worker 只返回有界候选索引与分数，默认关闭。

验收：

- [x] 可配置 Alibaba Embedding + DeepSeek LLM，或 Ollama Embedding + Google Gemini LLM。
- [x] LLM 临时故障可使用显式备用 provider。
- [x] Rerank 关闭时完整流程仍正常。

---

## 阶段 11：查询 API

- [x] `POST /v1/knowledge/query`。
- [x] 输入长度和字符规则校验。
- [x] 用户级和 tenant 级限流。
- [x] Query Embedding。
- [x] ACL Top K 检索。
- [x] 相邻块合并和去重。
- [x] 可选 Rerank。
- [x] 相关度阈值判断。
- [x] LLM 回答。
- [x] 引用校验。
- [x] 等价版本号问法规范化（如 `vue2` / `vue 2`）并贯穿 Embedding、Rerank 和 LLM。
- [x] 继续会话时按 tenant + user 验证所有权，只使用服务端记录敏感度与当前问题一致、且有界的近期用户问题联动检索与 LLM 指代消解；历史助手答案不重新出网，旧版敏感度未知的轮次失败关闭，独立问题不无条件拼接历史。
- [x] 新增 Rerank 后独立 LLM 上下文总预算，默认从最高 120,000 字符候选收敛到 32,000 字符；相邻块合并不得跨 PDF 页或工作表，确保来源位置与正文一致；移除被 active/ACL 分块查询覆盖的早期重复数据库鉴权，同时保留 LLM 前与返回前复核。
- [x] 首次引用不可核验时使用同一授权上下文受控修复一次；失败后混合模式切换为明确标记的通用知识回答，严格模式保持安全拒答并记录独立审计错误码。
- [x] 实现 `strict | hybrid` 回答配置（默认 hybrid）、`grounded | general` 响应契约、问题出网策略、历史/审计模式记录和通用回答前端提示。
- [x] 返回 traceId、来源和模型标识。
- [x] 审计记录。
- [x] CAD 文档线索、标题字段联合召回、短中文续问、候选去重和资料不足拒答已收口；保持服务端 document scope、tenant/ACL 过滤和引用复核。
- [x] 问答热路径与 Query Embedding 缓存优化已完成，并记录分阶段耗时、缓存命中和引用修复指标。

验收：

- [x] 正确问题返回答案与真实来源。
- [x] 通用问题无知识库依据时返回明确标记的通用知识补充；权限变化、内部事实不可确认或严格模式时安全拒答。
- [x] 删除/失效文档不作为来源。
- [x] 问题和正文不以明文进入普通日志。

---

## 阶段 12：删除、版本和重建索引

- [x] 删除原文件。
- [x] 删除全部向量。
- [x] 删除 Embedding 缓存中的可识别内容（根据 chunk 关联 cache key 清理 tenant 内缓存）。
- [x] 删除补偿可恢复：`deleting` 墓碑在授权列表可见并可重复调用幂等 DELETE；删除期间尚未结束的解析结果会被丢弃并回收确定性预览产物；CAD bundle/瓦片目录保持 `previews` 共享组可写与 setgid，API 无需 root 即可递归清理。
- [x] 保留合规审计事件。
- [x] 新文档版本完整成功后原子激活。
- [x] 实现 reindex API/任务。
- [x] 新旧 collection 双写或离线重建方案。
- [x] 验证后切换 active collection。
- [x] 保留回滚窗口。

验收：

- [x] 切换 Embedding provider/model/dimensions 能安全重建。
- [x] 重建失败不影响旧索引查询。

---

## 阶段 13：可观测性和审计

- [x] 结构化审计事件。
- [x] HTTP P50/P95/P99。
- [x] Provider 延迟、429、5xx 和重试。
- [x] 模型用量和成本。
- [x] 将问答期间的 Provider token 与调用时估算成本按 tenant/trace 持久化，并接入用量页；迁移前历史和不完整 Provider usage 保持未知，页面缺失值统一展示“暂无数据”。
- [x] 队列长度和任务耗时。
- [x] 解析失败率和 OCR warning。
- [x] 检索空结果率。
- [x] Rerank 降级率。
- [x] 磁盘和 Chroma 健康状态。
- [x] `/metrics`。
- [x] 告警规则说明。

---

</details>

---

## 阶段 14：质量评测

已用 44 条本机真实技术资料问题完成受控 DeepSeek 双策略评测，其中 answerable/no_answer/unauthorized 为 32/6/6。Vector Top 5 的 Recall@5/MRR/引用正确率为 `0.71875/0.651042/0.884615`，P95 为 `4049 ms`，平均单题成本 `$0.000297`；Top 20 + 本地 BGE Top 5 的最终 Recall@5/MRR/引用正确率为 `0.71875/0.520833/0.88`，P95 为 `18680 ms`，平均单题成本 `$0.000432`。两轮无答案拒答率均为 1、越权泄露率为 0、错误率为 0、成本覆盖率为 1。候选没有 Recall 增益，MRR、延迟和成本均恶化，因此决策为 `keep_disabled`，Rerank 继续默认关闭；基线引用正确率低于本次 `0.9` 门槛，作为上线前质量风险保留。

上述报告生成后修复了邻块跨 PDF 页/工作表合并问题，当前 Rerank/LLM 上下文边界已经变化。旧报告保留为历史证据，但不再代表当前版本的上线基线；在再次完成受控评测前继续保持 Rerank 关闭。

- [x] 收集 30–100 条真实问题。
- [x] 标注正确答案、目标文档和页码/工作表/稳定分块。
- [x] 加入无答案问题。
- [x] 加入无权限问题。
- [x] 计算 Recall@K。
- [x] 计算 MRR。
- [x] 计算引用正确率。
- [x] 计算无答案拒答率。
- [x] 验证越权率为 0。
- [x] 记录 P95 延迟和单次成本。
- [x] 对比向量 Top 5 与 Top 20 + Rerank Top 5。
- [x] 基于评测决定是否默认启用 Rerank。
- [ ] 跨页/工作表切组修复后，使用同一 44 条私有数据集重新执行两轮质量、延迟与成本基线评测。

---

<details>
<summary>已完成阶段 15：Vue 前端（展开查看完整 checklist）</summary>

## 阶段 15：Vue 前端

详细页面、交互和验收要求见 [03-前端产品与界面设计.md](./03-前端产品与界面设计.md)。

- [x] 创建 Vue 3 + TypeScript + Vite 应用。
- [x] 配置 Vue Router、Pinia 和 Element Plus。
- [x] 实现 AppShell、登录状态、路由权限和统一 API client。
  - [x] 支持受控账号密码登录、服务端 HttpOnly session、登出与密码模式登录页；OIDC/JWT 与开发身份模式保持可选。
- [x] 实现知识问答、来源卡片和响应式来源详情；PC / Pad 使用右侧抽屉，Mobile 使用带全视口遮罩、拖拽把手和独立关闭按钮的底部弹层。仅展示正文实际引用的来源，并在当前登录会话跨导航连续保留已完成轮次，直至点击“新建问答”。
- [x] 实时回答与问答历史支持点击行内 `[来源N]` 打开同编号来源详情，并保留底部来源卡片；历史来源返回前重新校验运行时契约、当前 ACL、文档状态和 active version，任一来源失效时整轮历史回答安全失效。
  - [x] `answer-text` 与 `history-answer` 复用受控 Markdown 组件，支持常用排版和 `[来源N]`，并通过标签、属性、链接协议白名单阻止原始 HTML、脚本、iframe、图片、事件属性和危险链接。
  - [x] `SafeMarkdown` 支持 KaTeX 本地公式：`\\[...\\]`、`$$...$$` 块公式与 `\\(...\\)`、`$...$` 行内公式，并兼容 `[\\ ... \\]` 的模型输出变体；围栏代码块保持字面代码，但会修复模型遗留的“未闭合且紧随块公式”围栏。LLM 提示词禁止以围栏包裹公式。公式渲染关闭 trust 扩展并限制尺寸/扩展次数，长块公式可横向滚动且不放宽 Markdown HTML 安全边界。
  - [x] 修复 `SafeMarkdown` scoped 样式无法命中 `v-html` 动态节点的回归；表格恢复原生 table 布局、对齐和可聚焦的块内横向滚动，并补齐 1–6 级标题、非 1 起始有序列表、分隔线、换行、粗体、斜体和删除线回归。
- [x] 实现文档列表、上传、入库任务和文档详情。
  - [x] 正式文档列表、分页、筛选与上传选项契约。
  - [x] Vue 文档表格、URL 筛选、错误重试与单文件上传入口。
  - [x] ACL 入库任务列表/重试、文档详情/版本、删除与重建契约及 Vue 页面；详情显示每个版本的向量 collection 标识、向量数和索引时间，并提供 ACL 二次校验、版本选择和分页的分块详情页，展示原始/脱敏文本但不泄露向量值。
  - [x] 文档分块卡片视觉层级收口：标题、位置、Chunk ID、结构化 metadata 与正文分区展示；metadata 复用 `kb-data-fields` 与无分隔线 modifier，相邻 Chunk ID 按上一个/下一个分行并允许 fingerprint 安全换行；原始/脱敏正文使用可区分的中性灰和低饱和蓝灰背景，Desktop/Pad 双栏，Mobile 通过 Element Plus Tabs 一次只挂载一份正文。
  - [x] 多文件上传与可授权 metadata 修改。
  - [x] 实现全部当前上传白名单格式的 ACL 预览：PDF/图片/文本直读，DOCX/XLSX 本地转 PDF，DXF/DWG 本地转安全 SVG 或按需 PNG 瓦片，失败时降级为解析文本；补齐 preview volume、删除清理、Range 读取、共享契约和回归测试。
  - [x] 修复 Office/CAD 中文方块字：Worker 镜像加入 Noto CJK，Office PDF 嵌入字体，ezdxf 默认缺字字体（含 Ubuntu runner 的 DejaVu Sans Condensed 变体）切换为 CJK 后输出 SVG 路径；DXF/DWG 增加可缩放与鼠标拖拽平移，全部预览增加全屏，权限说明收敛为同行状态标记。
  - [x] SVG 与瓦片 CAD 预览统一为 50%–409600% 默认范围，按钮使用 1.5 倍率、Ctrl/Command + 滚轮使用 1.25 倍率，并补充 SVG 与 manifest 受控上限回归测试。
  - [x] 修复复杂 CAD 预览：SVG 几何路径使用非缩放线宽并将零宽 hairline 提升为屏幕 1px；原始 SVG 超过 200 MiB 时确定性 gzip 存储，API 以 `Content-Encoding: gzip` 透明返回，压缩后仍超限才降级解析文本。
  - [x] 修复小型 CAD SVG 被超粗白线覆盖：按物理尺寸/viewBox 换算 style 与属性线宽并限制为 1–8 CSS px，再应用非缩放线宽；序列化使用默认 SVG namespace，并增加数千 CAD 单位线宽回归。
  - [x] 补齐 CAD 瓦片分流：源字节和估算成本未命中时继续检查清洗后的实际 SVG 负载，单体 SVG 超过 8 MiB 自动升级为按视口 PNG 瓦片，避免小 DWG 膨胀为超大 SVG 后缩放卡顿并缺少瓦片主体鸟瞰交互。
  - [x] 超大 CAD 预览改为渲染成本分流：递归计入嵌套块与阵列实例，入库只生成总览/z0、实体/扁平图元 R-Tree 和带 `worldToPixel` 的 manifest，不全量预生成细节 PNG；细节瓦片按视口懒渲染并用磁盘 LRU 清理，Worker 子进程受超时/内存上限保护。
  - [x] 前端 Canvas 瓦片查看器支持深度缩放、平移、全屏、最终视口防抖、可见瓦片优先、最多 2 并发、离开视口请求取消、边缘一圈预取和 `ImageBitmap` LRU 释放；API 在瓦片渲染前后复核 ACL，删除文档时递归清理 `.cad` bundle。
  - [x] 针对真实复杂 DWG 回归优化：bundle 初始化使用 3 倍单瓦片预算并封顶 180 秒、z0 复用总览、缓存命中绕过渲染锁，前端可见瓦片改为视口中心优先；初始化失败返回稳定细分 warning，并由父进程回收被强杀子进程遗留的 UUID 临时 bundle。
  - [x] 修复渐进 CAD 首次总览固定灰白：轻量 ASCII 扫描解析 LAYER、True Color、ACI 与可见性，binary DXF 使用 RenderContext 同口径，在完整几何尚未建立时就生成彩色 `overview.png` / `overview-geometry.sqlite`，首次细节后仍由完整块展开结果原子替换。
  - [x] 修复中等复杂度渐进 CAD 首次总览缺块内边框：源 DXF `<=64 MiB` 且加权成本 `<=500,000` 时用低分辨率 Frontend 在 500,000 图元硬上限内展开块引用；更大图继续使用直接实体快速降级，高层级完整索引仍只在首次细节请求建立。
  - [x] 修复块展开总览仍被旧 ASCII bounds 裁切：中等复杂度路径用包含 `INSERT` 内容的模型空间 bbox 重算 manifest 完整边界与可见主体样本，临时空间索引不发布；快速索引、总览和后续瓦片复用同一世界范围。
  - [x] 收敛受控渐进 CAD 的首次/回归 100% 几何事实：转换 DXF `<=128 MiB` 且加权成本 `<=1,000,000` 时，在 reindex 内按 `min(3×单瓦片预算, 150 秒)`、配置内存、400 万图元和 1 GiB bundle 上限尝试预建最终索引；成功后首次总览直接来自最终几何，失败安全保留快速总览。
  - [x] 消除 CAD 冷瓦片重复解析：入库生成非可执行 SQLite/R-Tree 扁平图元索引，旧 bundle 首次请求原子补建；3×3 metatile 改为一次查询、一次组合绘制后裁图。用 184,969 实体真实 DWG 回归，z4 冷 3×3 从约 31 秒降至 2.97 秒，z8 冷 3×3 为 1.10 秒，浏览器连续放大至 506% 后 5 秒内完成清晰细节加载。
  - [x] 修复远距辅助实体稀释 CAD 最大细节：完整模型空间 bounds 与全部图元继续保留，使用最多 4096 个 bbox 的确定性蓄水池样本和两端 1% 稳健范围估计主要几何跨度；完整跨度至少放大 2 倍时按倍率将单图 `maxZoom` 从配置值动态提高且硬封顶 12。该修复上线时真实教学楼 DWG 从 z8 提升到 z12、目录系统图保持当时的 z8；后续默认层级已统一提升到 z12。
  - [x] CAD 默认细节上限统一提升为 z12（相对总览 4096×），解决多个相距较远的有效图组不属于离群值、但 z8 仍不足以辨认细节的图纸；瓦片继续只按实际视口懒加载。SVG 与瓦片 CAD 均增加带当前视窗框的鸟瞰图，支持点击、拖动与方向键定位。
  - [x] SVG CAD 缩放改用与瓦片一致的相机锚点语义：按钮连续缩放保持当前视窗中心，Ctrl/Command + 滚轮保持指针下的内容点，缩放完成后同步滚动坐标与鸟瞰视窗，避免简单图纸放大缩小时逐步跑出可视区。
- [x] 实现 403、404、空状态、错误状态和重试。
  - [x] 文档管理加载失败时，错误态与表格/空态互斥渲染，不再同时显示空表头和“暂无符合条件的文档”。
- [x] 实现用户角色、部门权限、审计和系统管理页面。
  - [x] 对接 tenant 隔离、`audit:read` 强制鉴权的正式审计事件 API 并实现审计中心页面。
  - [x] 审计中心“操作”列对当前后端产生的问答、权限、文档索引与运行配置事件统一使用简体中文，未知事件使用中文兜底而不回显英文事件码。
  - [x] 审计事件 API 返回 tenant 与类型筛选范围的准确 `total`；游标分页仅开放已读取页及下一页，分页组件持续使用该 `total` 展示记录总数，不得由游标页数推算。
  - [x] 用量页在零查询时仍展示当前 Embedding Provider/model，并将查询审计聚合明确标记为“涉及问答”而非供应商请求；知识问答审计只展示实际 LLM，入库云端策略事件固化并展示当时的 Embedding Provider/model，历史事件按可验证 collection 事实回填已支持模型。
  - [x] 对接独立 `system:read` capability 的 Provider 与系统状态只读 API，并实现对应页面。
- [x] 对接 tenant/部门隔离的用户目录 API，并实现搜索、筛选、分页以及本地后台账号的创建、编辑、禁用、密码重置和删除页面；用户与角色、部门权限导航入口具有点击回归覆盖，窄屏仍可访问。
  - [x] 角色 mutation、部门权限和用量页面。
- [x] 应用角色收敛为 `user`/`admin`；旧 `platform_admin` 迁移为 `admin`，其他旧角色降为 `user`。`admin` 自动拥有当前 tenant 的全部 capability 与敏感度访问，tenant 和出网策略继续独立强制校验。
  - [x] 普通用户导航和前端路由只开放知识问答、问答历史；其余页面同时要求 `admin` 与对应 capability。
- [x] 完成管理员运行配置版本与受控发布：LLM/Rerank/问答/上传入库/Parser/Tika/CAD/DWG 字段白名单校验、密钥 AES-256-GCM 加密且不回显、配置与发布审计、独立 Docker socket 部署代理、指定服务 Compose 重建、readiness 验证、失败自动恢复与人工回滚；发布记录返回变更原因，Parser/CAD 变更同时固定重建常规与 DWG Worker；Embedding 变更继续强制走索引迁移。
  - [x] 修复手动 Compose 重建时 `.env` 反向覆盖版本化 `runtime.env` 的配置漂移；基础模式固定关闭 DWG，完整模式按受控配置决定上传与路由。Provider 长表单拆分 Parser、CAD/DWG、Tika 并增加响应式 Anchor，发布记录改为固定表头和块内滚动。
- [x] 完成组件测试和关键流程 Playwright 测试。
- [x] 完成桌面响应式和基础可访问性验收。
  - [x] 页面标题、工具栏与内容块分层：应用壳层和问答工作台的 `kb-page-header` 统一使用 Element Plus `el-page-header` 的 `content` / `extra` 插槽；所有管理页内容在各自块内滚动，`xxxx-page` 填满可用高度，不依赖 toolbar sticky 定位。
  - [x] Mobile 应用壳层不渲染 `.kb-title-group`；无返回操作时不保留空页头，文档详情等来源路由仍保留返回入口。
  - [x] Mobile 来源路由不再为返回操作单独渲染 `el-page-header__header`；ArrowLeft 图标返回入口收入固定 `app-header`，位于汉堡按钮与当前页标题之间，以 `aria-label` / `title` 保留完整返回文案；页标题允许收缩并省略，PC / Pad `el-page-header` 不变。
  - [x] 375px 移动端回归：Drawer 保留全部已授权导航入口；系统/用量/历史页面、日期范围控件与 Provider 详情均不溢出；手机表格使用字段卡片，桌面/平板表格与可滚动列表在各自内容块内冻结表头或标题，页面本身不产生横向滚动。
  - [x] 细化 375px 与 901–1279px 布局：文档筛选可弹性换行且查询/重置成组，Drawer 与弹框遮罩覆盖 shell，Drawer 顶栏和 app header 高度统一；详情操作区具备独立卡片背景、边框与圆角。信息字段统一改用 Element 描述组件或 Flex/Grid，移除 `dl/dt/dd` 布局依赖。
  - [x] 修复上传弹框遮罩遗漏侧栏、审计表外层滚动导致表头失效，以及 375px 下历史/审计筛选控件收缩或独占一行的问题；审计表改由 Element Table 自身滚动并固定表头。
  - [x] 将 768–900px 也切换为 Drawer 导航，消除仅图标侧栏；所有弹窗挂载到 `body` 并限制移动端宽高，避免遮罩或内容越界。
  - [x] 修复合法 UTF-8 文本跨 8 KiB 校验分块时被误判、上传弹窗复用上次文件，以及整批上传成功后弹窗不关闭的问题。
  - [x] 对齐上传文件状态标签；上传多文件时列表在弹框块内滚动且不产生页面 Y 轴滚动；进行中入库任务耗时每秒刷新、结束后冻结；按 774,303 实体复杂 DWG 样本将默认遍历上限受控提高至 1,000,000，并将 Worker 的 allowlist 安全错误码透传到任务详情。
  - [x] 响应式规范收敛：唯一断点 `1280/900/768`、统一 `useBreakpoint()`、移动端表格结构性切换为卡片、筛选 Drawer、手机 Dialog/Drawer 适配与 44px 触控约束。
  - [x] UX/UI 重构基线：断点收敛为 `1280/768`；PC 使用“问答 / 知识库 / 安全与权限 / 系统”四组不可折叠的展开侧栏，Pad 保留全部入口图标、组间分隔线和由 Element Plus `el-breadcrumb` 渲染的顶部面包屑，hover/键盘聚焦只使用 Tooltip 显示当前入口名称，点击直接导航且不创建二级 flyout，`app-sidebar` 在短视口下支持 Y 轴滚动；Mobile 通过顶部汉堡按钮从左侧展开/收起与 PC 同分组的完整 sidebar，不保留底部导航或为其预留页面高度，侧栏保留身份摘要和安全退出；文档管理在 Pad 以下切换为卡片列表。
  - [x] 主导航通过路由 `meta.activeNavigation` 显式维护模块归属；文档详情、预览和分块子页在 PC、Pad 与 Mobile sidebar 中持续选中“文档管理”，当前入口同步设置 `aria-current="page"`。
  - [x] Mobile sidebar 顶部层级收口：品牌与关闭按钮位于首行，用户头像、用户名与部门收入第二行独立身份卡，长用户名不得挤压 Logo 或关闭入口。
  - [x] 问答历史三档交互：PC 与 Pad 保留会话搜索和筛选/重置操作；PC 保留 hover 删除图标及列表/详情一体式卡片，Pad 保持窄列表双栏。Mobile 使用顶部菜单页头、搜索 + 重置与列表/详情两级全宽卡片，不显示“筛选”按钮，并保留键盘可达的会话和删除入口。会话列表直接复用 `kb-block-content` / `kb-block-scroll`，整行 `div` 不声明 button/listitem role，保留焦点与 Enter/Space 操作，标题/摘要使用 `span + kb-text*`。三端均移除开始/结束时间、历史筛选 Drawer 和页码分页，上划至列表底部附近时使用 `offset/limit` 追加下一批；无数据时 `el-empty` 在列表剩余空间双轴居中。
  - [x] 历史与部门目录滚动基线收敛：问答历史和 PC / Pad 部门目录的真实滚动节点均只组合 `kb-block-scroll + kb-block-scroll--list`，共用滚动、overscroll、内距和 scrollbar gutter，不保留仅供测试定位的页面 class。部门整行入口改为不声明 button role / `aria-pressed` 的 `div`，保留焦点和 Enter/Space 操作，移除 Element Button wrapper 所需的 `:deep()` 规则。
  - [x] 问答历史 Mobile 详情的“← 返回会话列表”入口移入应用壳层 `kb-action-group`，与文档详情、文档分块统一位置和视觉规格；返回时仅移除 `conversationId`，保留历史筛选。
  - [x] 文档工具栏三档交互：PC 单行、Pad 两行，Mobile 使用“搜索 + 上传 + 筛选”布局；手机筛选 Drawer 只承载状态、敏感度和格式，搜索框始终保留在工具栏。
  - [x] 入库任务工具栏三档交互：PC 与 Pad 均为“文档 ID + 状态 + 筛选/重置”单行，Mobile 保留文档 ID 输入和文字“筛选”入口，状态收入底部 Drawer 且不重复文档 ID 输入框。入库卡片与工具栏专属样式收敛到 `IngestionJobsView.vue` scoped 样式，内容面板使用圆角裁剪内部滚动区和分页。
  - [x] 移动筛选重置、错误态与 surface 修复：文档、入库和用户 Drawer 的“重置”清空工具栏与 Drawer 全部条件并立即重新加载；入库文档 ID 在请求前校验 UUID，字段/API 错误态隐藏旧分页；Mobile `access-table-wrap` / `department-layout` 恢复白色 `kb-block` surface 并使用 `kb-block-scroll` 独立纵向滚动，历史空态填满剩余高度并居中。
  - [x] 历史/文档/筛选细节回归：PC/Pad 问答历史新搜索后清空右侧旧详情；PC/Pad 文档空态改为带 `el-empty__image` 的完整空态；文档详情展示并可复制文档 UUID；Mobile 用户/部门外层保留单一边框、内层 Collapse 去边框；文档/入库/用户筛选按钮统一依 Drawer 条件显示 `is-active` 圆点。
  - [x] 入库任务长列表收起：进行中与失败任务默认展开，完成及其他终态默认收起；收起行只挂载进度点、状态和摘要，支持整行及键盘切换。Pad 展开步骤使用自带完整背景的块内横向滚动区，Mobile 保留垂直步骤清单，长文件名省略且禁止任务列表 X 轴溢出。
  - [x] 入库状态、筛选选项与任务类型展示字典统一由 `ingestion-presentation.ts` 维护，页面不直接回显英文任务类型值。
  - [x] 上传弹层三端收口：PC/Pad 分别使用约 460/400px 居中 Dialog 并保留拖放区；Mobile 使用全屏底部卡片、整行“选择文件”、可滚动正文和等宽吸底操作。三端文件队列统一为上传中/失败/已排队卡片，上传请求通过带凭据 XHR 显示真实字节进度。
  - [x] 文档详情三端收口：PC 基本信息与当前向量索引双卡并排，Pad 纵向堆叠；Mobile 将预览、修改权限、重新索引与红色删除项收进右上角“⋯”底部操作面板，版本历史切换为卡片，并将 DWG 转换 warning 合并为可读的格式转换说明。
  - [x] 文档高风险确认统一：删除待清理的“继续清理删除”、永久删除与重新/继续建立索引共用同一文档名强确认组件；桌面使用限宽 Dialog，Mobile 使用底部 Drawer，风险说明、完整文件名和确认按钮层级一致。
  - [x] 文档移动操作面板的预览链接与三个 Element Button 使用同一三列布局，Element Button wrapper 覆盖限定到 `.el-button.mobile-action-item`，避免预览行右侧箭头跟随文字。面板保留原生链接/按钮语义，不声明未实现完整键盘模型的 `menu/menuitem` role。
  - [x] Mobile 全类型预览工具栏使用统一紧凑骨架：文件身份/权限标记在左，全屏固定右上；PDF、图片、Office 转 PDF、文本/Markdown 不保留空的操作行，CAD 才追加三列等宽缩放行。引用位置存在时单独占行，交互错误改为工具栏内正常流，长文件名需为右上全屏保留安全宽度，禁止工具栏水平溢出。
  - [x] 截图显示回归：审计/用户表格与空状态改为互斥渲染，历史会话列表在卡片内滚动并上划追加；纵向任务步骤条覆盖 Element Plus 默认满高，部门移动端卡片禁止 Grid 自动行拉伸，详情操作和 Provider 内容在受限宽度内换行或滚动。
  - [x] 分页与移动操作收敛：列表分页使用统一底部容器；审计 cursor API 在前端维护游标页栈并展示上一页/页码/下一页。手机筛选按钮位于所属工具栏右侧，底部筛选 Drawer 使用 `size="auto"` 随内容收敛并保留安全视口滚动；用户和部门摘要使用可展开卡片，详情操作仅在展开后出现。
  - [x] 权限卡片收敛：手机端用户与托管角色从展开卡片触发底部 Drawer 编辑，部门敏感度策略保持卡片内联编辑；桌面端角色编辑使用标准 Dialog。历史列表不显示分页，审计数据区保持带边框滚动块，统一分页在边框外使用透明背景。
  - [x] 上传与 metadata 弹框回归：补齐 Dialog/Drawer 的 Element Plus 基础样式，统一桌面限宽居中、手机底部 Drawer 高度、正文滚动和 footer 可见性，并覆盖 375px 上传与 metadata 尺寸回归。
  - [x] 短视口 Dialog 滚动边界：全局 Element Dialog 使用纵向 Flex 结构，header/footer 固定、body 独立纵向滚动；上传文档、新增账号和管理后台账号在 360px 高度下均不越界且可滚动到完整内容。
  - [x] Provider 页面高度与滚动回归：页面恢复纵向 Flex 布局，长表单由 `.kb-page__content` 在应用壳层内独立滚动，避免被主内容容器裁切。
  - [x] 前端样式归属收口：`main.css` 只保留 reset、全局无障碍/动效、回答来源/Markdown 与 Element Plus Teleport 浮层规则；页面和业务组件样式先分别迁入对应 Vue SFC，跨页面公共 CSS 后续再按稳定模式抽象。
  - [x] 前端业务 CSS 选择器统一为语义 class：原生标签保持不变并追加“父容器语义 + 元素用途”class，Markdown 动态节点和相关脚本查询同步改用 class；全局 reset/base 与第三方组件内部样式继续作为明确例外。
  - [x] Element Plus 全局通用覆盖集中到 `styles/element.css` 并由入口单次引入；页面专属的“业务 class + `.el-*`”规则下沉到对应 SFC 的 scoped 样式，只在必须命中第三方内部 DOM 时使用范围最小的 `:deep()`；跨页面稳定复用的 Drawer、Collapse 与日期浮层基线继续由全局原生类或语义变体统一维护。
  - [x] Element Plus 2.14.3 light/dark theme 按官方 `--el-*` 变量重新映射 `--kb-*` token；根级 ConfigProvider 三端固定使用 `default`，所有表单与操作控件统一为默认高度，Tabs、Dialog、Drawer、Collapse、Upload 等组件使用成套 CSS 兜底。系统深色偏好由 CSS media query 驱动，顶栏菜单可用本地 UI 偏好覆盖或清除覆盖恢复跟随系统；状态色、Markdown、分块对比和加载遮罩均使用语义 token。
  - [x] 修复 Mobile DatePicker 外层 popper 已扩展至安全视口、内部面板仍保留 Element Plus 默认定宽导致内容偏左的问题；面板与日历内容改为填满浮层并保持对称内距，根级 ConfigProvider 实际传入简体中文 locale，年月、星期与操作按钮不再显示英文。
  - [x] Element Plus 2.14.3 Button、Input、Select、DatePicker、InputNumber 等控件高度与圆角统一：PC / Pad / Mobile 均使用 40px 默认高度；页面 `kb-block` 公共容器使用 `--kb-radius-lg`，普通 Element 控件使用 `--kb-radius-md`，语义 pill/circle/Tag/bottom sheet 保留专用圆角；三端浏览器计算尺寸回归同时覆盖高度和圆角层级。
  - [x] Mobile 输入防自动缩放：Input、Textarea、Select、DatePicker 与 Range 的可聚焦输入文字统一使用 16px 语义 token，避免 iOS 聚焦时放大页面；viewport 继续允许用户主动缩放。
  - [x] 全站 opacity 收敛为 hidden、decorative、disabled、muted、visible 五个语义 token；业务与全局样式无裸 opacity 数值，并由 Stylelint 防止回归。
  - [x] Element Plus 相邻按钮间距收口：全局 `.el-button + .el-button` 左外边距归零，工具栏、表格行操作、Dialog/Drawer footer、移动筛选与 CAD 缩放按钮组统一改由父容器 `gap` 控制，并覆盖 PC、Pad、Mobile 三端回归。
  - [x] 全站 12px 间距语义收口：页面与业务组件优先使用 `--kb-layout-gap`、`--kb-list-row-padding`、`--kb-block-padding`，块级基础内距统一为 12px；业务样式不再直接引用 `--kb-space-3`。
  - [x] 全站 Drawer 视觉壳层统一：header/body/footer、标题与关闭按钮、分隔线、背景、阴影、安全区和底部把手由 `element.css` 集中提供，业务组件只保留方向、尺寸、最大高度和正文布局差异。
  - [x] 共享反馈与弹层 Close 收口：`kb-error-state` 不再依赖外层 `kb-block`，自身统一 danger-soft 背景、边框、圆角与 600 标题字重；Dialog/Drawer Close 统一为带边框的圆形控件并共用 hover/focus 规则。
  - [x] Dialog、Drawer 与 Mobile sidebar 自定义 Close 的 hover/focus/active 色阶完全对齐；边框全程保持中性色，hover/focus 使用 primary-soft、active 使用 nav-accent 背景。
  - [x] 数据字段卡片语义收敛：审计、用量、用户目录、文档详情与 Provider 摘要统一复用 `kb-data-*` 结构和 `kb-block` 基线；`kb-data-field__label/value` 与 `kb-data-grid__value` 分别内联各自结构的稳定文字基线，模板只叠加真正的状态/强调 modifier；移除按设备命名的字段卡片 class 与页面重复视觉规则。块级容器、共享 modifier 和内部结构统一使用 `kb-block-*`，移动端可展开列表统一复用 `kb-collapse-list`。
  - [x] 审计表格展开与入库任务详情统一使用 `kb-data-grid` 规格：共用布局、间距、内距、标签/值层级和 Mobile 两列布局，不添加 border、border-radius 或 background；审计保留三列，任务保留四列，不再保留无样式页面 alias。
  - [x] 系统状态摘要使用三列零内距 `kb-data-grid`，三个直接子项分别使用 `kb-block` border；队列使用五列零内距 `kb-data-grid` 且由外层 `kb-block` 提供容器边界；五列在 Pad 收为三列，两块在 Mobile 均收为两列。
  - [x] 用量摘要使用三列零内距 `kb-data-grid`，三个直接子项各自使用 `kb-block` border，Mobile 收为两列且数值保留 20px 强调；系统 `component-card` 改为“状态点 + 标题/说明”两列 Grid，统一最小高度、收缩与换行。任务与队列共用 `kb-data-grid--flush` 将 padding 设为 0。
  - [x] 用量页 DatePicker 宽度与交互统一：PC、Pad、Mobile 三端统一使用直接位于工具栏的单面板日期范围并移除筛选 Drawer；PC/Pad 对齐 `audit-filter-form` 的 Flex 紧凑排列并恢复 daterange 标准宽度，Mobile 填满操作按钮外的可收缩列；浮层与触发器等宽、左侧对齐，三端单面板、body-wrapper、body 与 content 均为 Popper 的 100% 宽度并清除内部 322px 最小宽度，各端均不产生 X 轴溢出。筛选收敛为不可输入的日粒度日历选择，并按本地自然日覆盖完整起止日期。
  - [x] 移动筛选与 Element 浮层收口：审计事件类型 Select/重置内联至 `audit-toolbar` 并移除 Drawer，仍使用筛选入口的页面统一 `kb-filter-trigger`，搜索 Input 移除前置搜索图标；Element 浮层统一 surface/边框/圆角/阴影与交互色，根级启用简体中文 locale，用量日期面板 Teleport 到高层级安全视口浮层并使用单面板以避免裁剪和横向溢出。
  - [x] 用户目录与用量工具栏对齐文档管理三端节奏：删除重复标题/说明；用户目录在 PC 与 Pad 均为“搜索 + 部门 + 筛选/重置 + 新增账号”单行布局，Mobile 使用“搜索 + 新增账号 + 筛选”且 Drawer 只保留部门；用量页在 PC 与 Pad 单行，Mobile 直接展示日期范围与重置，不再使用筛选入口或 Drawer。
  - [x] 列表与管理页内容容器收敛：`kb-page__content` 专用于多区块整页滚动，`kb-block-content` / `kb-block-scroll` 用于固定外壳和内部列表滚动；文档管理与审计中心的 Mobile 外层内容壳与滚动区保持零内距，由列表卡片自身提供 padding，入库任务、文档分块与用户目录复用结构但保留各自内距。所有页码分页统一置于对应 `kb-block-content` 底部，不作为 `kb-page` 的直接子节点；带边框滚动块与透明分页为同级，分页不进入边框且不额外添加上边框。预览降级列表同样使用内部滚动区与固定底部分页。
  - [x] Desktop 管理表格双底边收口：无内距边框块关闭 Element Table 的底部伪元素、固定列底部补丁及最后一行单元格底边，由外层 `kb-block` 为文档管理、审计中心和用户与角色提供唯一终止线，其他行分隔线继续保留。
  - [x] 全量前端 classname 审计：移除没有样式、脚本行为或必要测试定位用途的静态 class，删除无对应节点的孤立 CSS 选择器；页面内容容器、移动返回入口和响应式状态的 E2E 定位改用共享结构 class。文档分块移除无独立职责的 `chunks-content`，滚动区与分页继续置于同一 `kb-block-content` 外壳。
  - [x] 静态 class 二次收口：删除仅用于旧测试定位、对称命名或已经失去样式/行为职责的 class；单元测试与 E2E 改用共享结构、`aria-label`、可访问角色或稳定控件关系，不为测试重新增加生产 class。自动复扫要求 Vue 静态 class 无未引用候选，业务样式无孤立选择器。
  - [x] 全量 Vue 与测试一致性复核：逐个核对 SFC 模板、脚本和 scoped 样式，并同步 `*.test.ts` / `phase15.spec.ts` 的可访问名、共享 class、断点、控件高度、Drawer、分页、滚动和空态断言；移除来源抽屉废弃 class、`:global()` 与裸颜色，补回详情/分块/来源 heading，并让文档 ID 文本本身成为不额外显示按钮文案的点击复制入口，恢复 viewport 用户缩放；误复制为文档详情平行实现的 `ComingSoonView` 恢复为轻量占位页。
  - [x] Mobile 文档分块页改为整页纵向滚动：`chunks-toolbar` 随页面滚走，`kb-block-content` / `kb-block-scroll` 在 Mobile 不再被压缩到工具栏之下的剩余高度，版本选择器满宽且摘要左对齐；PC / Pad 继续使用固定工具栏和内部列表滚动。
  - [x] Vue CSS 二次精简：删除空 style/媒体查询/规则、重复公共声明和无效 Grid/Flex 属性；`main.css` 按基础、布局、块结构、文本、反馈、筛选与响应式顺序整理，并抽取 `kb-split-layout`、`kb-status-toolbar`、`kb-error-state`、`kb-inline-error`；业务状态 class 统一使用 `is-*`，文件队列、分块和来源标题改用父容器语义命名。
  - [x] `main.css` 公共 Class 全量收敛为 `kb-*` 命名空间与 BEM；页面、页头、块标题、筛选、回答来源、品牌、状态页、动态 Markdown 和测试定位同步迁移，不保留旧名兼容层。删除未使用或无效果规则，公共 modifier 与基础 Block 配对，并抽取跨页面 `kb-control-toolbar`、`kb-title-group` 与 `kb-action-group`；旧 `kb-caption` 已进一步并入正交的 `kb-text` 尺寸/颜色体系。
  - [x] 全站通用文字体系收口：`main.css` 统一提供 `kb-heading + h1…h6` 视觉层级与 `kb-text` 尺寸、颜色、字重 modifier；普通展示文字不再使用 `el-text`，字段/网格标签值与状态文字组合公共文字 class，Vue scoped CSS 只保留响应式、代码字体、截断及页面独有效果。按产品约束不使用 `p` / `h1`–`h6`，标题通过 `div/span + role=heading + aria-level` 保留语义。
  - [x] 文档预览页优先复用 `main.css` 已有的工具栏、页头操作、块内容、间距、滚动和警告文本结构；删除仅按 URL 页码/工作表给降级分块加边框、不承担定位或交互的 `is-referenced` 状态。
  - [x] 文档详情页根布局、标题操作栏、标题/操作区、危险与 warning 文本、最近任务字段行优先复用 `main.css` 已有的 `kb-page`、`kb-status-toolbar`、`kb-title-group`、`kb-action-group`、`kb-block__title`、`kb-text--*` 和 `kb-data-*`，保留双卡 Grid、DWG 说明和移动操作面板等页面专属样式。
  - [x] 文档详情 PC / Pad 与分块页使用一致的固定顶部行模型：`detail-actions` 作为 `document-detail-page` 直接子节点固定可见，只有下方 `detail-content.kb-page__content` 滚动；Mobile 由整个详情页滚动，操作卡可随页面移走。
  - [x] 文档、入库任务和详情页的文字型可点击入口统一改为 `el-link type="primary" underline="never"`；内部路由通过 `RouterLink custom` 传递 `href` / `navigate`，避免默认 `RouterLink` 包裹 `el-link` 产生嵌套锚点。全部 Vue 中的 `el-link` 共用 `kb-link` / `kb-link__text` 的左对齐、Flex 收缩与省略规则，按需使用 `kb-link--fill` / `kb-link--end`；不改动按钮、整卡、品牌和主导航语义。
  - [x] 问答输入区收口：`AskComposer` 使用图标发送、作用域提示、接近上限字数提示与提交中旋转状态；提交期间同时禁用文本输入和发送操作，并使用专属 `--kb-shadow-input` 作为输入焦点区域的可见阴影。组件与浏览器回归同步使用“发送问题”可访问名并覆盖禁用、阴影与聚焦边框。
  - [x] P0 回归收口：修复 901–1279px 详情操作换行、用量页工具栏方向和移动端工具栏覆盖问题，并完成当时的管理页工具栏对齐。Playwright 18/18、Parser 容器 40/40、隔离 tmpfs PostgreSQL/Redis/Chroma 集成测试 10/10 通过。通过已登录 Web 页面将 `Drawing1.dwg` 重建为 v2，确认 ODA→DXF→ezdxf、2 个向量分块与原子激活，并由当前 DeepSeek 主 LLM 返回带 `Drawing1.dwg` v2 引用的真实答案；Gemini 保持备用 Provider。

前端仅负责用户体验层权限控制，所有资源访问仍由后端强制鉴权。

---

</details>

---

## 阶段 16：服务器上线准备

- 阶段 4、5、11、15 已完成的解析、查询和预览专项收口不在本阶段重复列出；本阶段只追踪生产部署前置条件。
- [ ] 生产 Docker 镜像。
- [ ] 非 root、只读文件系统和最小权限。
- [ ] PostgreSQL 持久化。
- [ ] Redis 持久化策略。
- [ ] TLS 反向代理。
- [ ] OIDC/JWT。
  - [x] 浏览器 Authorization Code + PKCE 回调、内存 Bearer token 与 API client 注入已实现；仍需使用生产实际 IdP 完成 CORS、claims、过期与登出验收。
- [ ] Secret Manager/Vault。
- [ ] 备份脚本和恢复演练。
- [ ] 日志和指标采集。
- [ ] 压测。
- [ ] 安全测试。
- [ ] 数据保留与删除策略。
- [ ] 模型平台的数据与区域合规确认。

---

## 后续规划：Lite 模式（尚未进入实现）

- [ ] 明确 Lite 的目标用户、部署形态、功能矩阵和验收标准。
- [ ] 定义与现有 Compose `full` 命名不冲突的产品模式配置与运行时契约。
- [ ] 复用现有入库、Provider、向量检索、问答和安全核心，不创建平行业务实现。
- [ ] 通过服务端配置、路由与 capability 强制功能边界，前端导航只反映服务端能力。
- [ ] 补充模式切换、安全边界、数据兼容、升级/回退和端到端测试。

最终验收按 [01-项目实施规格.md](./01-项目实施规格.md) 第 10 节和 [03-前端产品与界面设计.md](./03-前端产品与界面设计.md) 第 27 节执行。
