openapi: 3.1.0
info:
  title: NexusKB API
  version: 0.3.0
paths:
  /v1/documents:
    post:
      summary: Upload a document and enqueue ingestion
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file: { type: string, format: binary }
      responses:
        '202':
          description: Queued
          content:
            application/json:
              schema: { $ref: '#/components/schemas/UploadAccepted' }
        '409':
          description: Content already exists in the same tenant and ACL scope
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ApiError' }
  /v1/documents/{documentId}:
    parameters:
      - $ref: '#/components/parameters/DocumentId'
    get:
      summary: Get a tenant-scoped document
      responses:
        '200': { description: Document metadata }
        '404': { $ref: '#/components/responses/NotFound' }
    delete:
      summary: Idempotently delete a tenant-scoped document
      responses:
        '200': { description: Deleted or already absent }
  /v1/ingestion-jobs/{jobId}:
    get:
      summary: Get a tenant-scoped ingestion job
      parameters:
        - name: jobId
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '200': { description: Ingestion job }
        '404': { $ref: '#/components/responses/NotFound' }
  /v1/ingestion-jobs/failed:
    get:
      summary: List recent failed ingestion jobs in the authenticated tenant
      responses:
        '200':
          description: Up to 50 failed jobs ordered by most recent update
          content:
            application/json:
              schema:
                type: object
                required: [jobs]
                properties:
                  jobs:
                    type: array
                    items: { $ref: '#/components/schemas/IngestionJobSummary' }
components:
  parameters:
    DocumentId:
      name: documentId
      in: path
      required: true
      schema: { type: string, format: uuid }
  schemas:
    UploadAccepted:
      type: object
      required: [documentId, jobId, status, traceId]
      properties:
        documentId: { type: string, format: uuid }
        jobId: { type: string, format: uuid }
        status: { const: queued }
        traceId: { type: string, format: uuid }
    ApiError:
      type: object
      required: [error]
      properties:
        error:
          type: object
          required: [code, message, traceId]
          properties:
            code: { type: string }
            message: { type: string }
            traceId: { type: string, format: uuid }
    DocumentStatus:
      type: string
      enum: [uploaded, processing, prepared, active, policy_blocked, failed, deleting, deleted]
    IngestionStatus:
      type: string
      enum:
        [
          queued,
          parsing,
          chunking,
          policy_check,
          embedding,
          indexing,
          policy_blocked,
          completed,
          failed,
          deleted,
        ]
    IngestionJobSummary:
      type: object
      required:
        [id, documentId, version, status, step, checkpoint, attempts, traceId, retryable, updatedAt]
      properties:
        id: { type: string, format: uuid }
        documentId: { type: string, format: uuid }
        version: { type: integer, minimum: 1 }
        status: { $ref: '#/components/schemas/IngestionStatus' }
        step: { type: string }
        checkpoint: { type: string }
        attempts: { type: integer, minimum: 0 }
        traceId: { type: string, format: uuid }
        errorCode: { type: [string, 'null'] }
        errorCategory: { type: [string, 'null'] }
        retryable: { type: boolean }
        startedAt: { type: [string, 'null'], format: date-time }
        completedAt: { type: [string, 'null'], format: date-time }
        updatedAt: { type: string, format: date-time }
  responses:
    NotFound:
      description: Resource does not exist in the authenticated tenant
      content:
        application/json:
          schema: { $ref: '#/components/schemas/ApiError' }
