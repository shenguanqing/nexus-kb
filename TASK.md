# 当前开发任务

> 项目：知枢 NexusKB；当前阶段：阶段 16——服务器上线准备；状态：进行中，前端、本地 RAG 主链路与阶段 14 真实质量评测已完成，剩余阶段 4/7 增强项已明确延期。

---

## 1. 当前目标

为 Linux 服务器上线完成可验证的最小准备。完整 checklist、依赖与勾选状态见 [05-开发任务清单.md](./docs/05-开发任务清单.md)。

- 固定生产镜像、非 root 与最小权限。
- 配置 TLS、认证、Secret Manager、PostgreSQL/Redis 持久化。
- 完成备份恢复演练、日志指标采集、压测与安全测试。
- 确认数据保留、删除策略和模型区域合规。

## 2. 质量结论与边界

阶段 14 已使用 44 条本机真实技术资料问题完成两轮 DeepSeek 受控评测：32 条可回答、6 条无答案、6 条无权限。私有数据集、身份映射、原始 run 与聚合报告均保持 Git 忽略和 `0600`；仓库只提交方法、代码与聚合结论。

- Vector Top 5：Recall@5 `0.71875`、MRR `0.651042`、引用正确率 `0.884615`、P95 `4049 ms`、平均单题成本 `$0.000297`。
- Vector Top 20 + 本地 BGE Top 5：最终 Recall@5 `0.71875`、MRR `0.520833`、引用正确率 `0.88`、P95 `18680 ms`、平均单题成本 `$0.000432`。
- 两轮无答案拒答率均为 `1`、越权泄露率均为 `0`、错误率均为 `0`、成本覆盖率均为 `1`。
- 候选未提升 Recall、MRR 回退 `0.130209`，P95 为基线 `4.613485` 倍且平均成本为 `1.454545` 倍，决策为 `keep_disabled`；Rerank 继续默认关闭。
- 基线引用正确率仍低于本次批准门槛 `0.9`，属于阶段 16 上线前需明确接受或继续优化的质量风险，不把“完成评测”表述为“所有质量门槛通过”。
- 上述指标采集于跨页邻块切组修复之前。当前实现已改变 Rerank/LLM 上下文边界，报告保留为历史证据但不再作为当前版本上线基线；正式上线前需按同一 44 条数据集重新执行两轮评测，重新确认质量、延迟、成本与 Rerank 决策。在重跑前继续保持 Rerank 关闭。

数据集、运行命令和决策门槛见 [`evaluation/README.md`](./evaluation/README.md)。

## 3. 阶段 16 前遗留与明确延期

以下原阶段 4/7 项目尚未实现，但不属于当前上传白名单或运行时 Provider 范围，因此不阻塞进入阶段 16：

- PPTX、HTML、RTF 和 EML 解析与预览。
- OpenAI Embedding 的运行时配置、Factory 接入和契约测试。

这些项目不得被描述为已交付。阶段 16 的压测、恢复演练或获批业务需求如证明其中任一项是生产上线前置条件，必须先提高优先级并完成实现、测试与文档；详细勾选项见 [开发任务清单](./docs/05-开发任务清单.md#阶段-16-前遗留与明确延期)。

## 4. 已完成基线

- 前端交付：Vue 前端、知识问答与来源、文档生命周期、管理后台、响应式与可访问性已完成。
- 本地 RAG 链路已覆盖 Alibaba、Google 和受控 Ollama Embedding、可配置 LLM、文本哈希向量复用、批次级 checkpoint、待建立索引恢复、DOC/DXF/DWG 解析和统一预览。
- API、Parser Worker、前端与 Compose 的安全边界、共享 OpenAPI/Zod 契约、迁移和自动化测试已建立。
- 文档职责已收敛：当前任务在本文，阶段路线图在任务清单，配置在 `.env.example`，接口字段在 OpenAPI。

历史实现细节、验证命令和提交记录以 Git 历史为准；当前架构、产品体验、运维与 API 规则分别见 `docs/01` 至 `docs/07`。

## 5. 最近确认

- 2026-08-28：修复 GitHub Actions CI run `33158175013` 的 `parser-worker` Mypy 失败。`_progressive_overview_needs_refresh()` 将 `json.loads()` 结果标注为 `Any`，使 `value != expected_marker` 也被推断为 `Any`，违反函数声明的 `bool` 返回类型；现收窄为 `object`，不改变运行时 marker 比较语义。使用与 CI 相同的 `pyproject.toml` 和 Pydantic Mypy 插件复跑 `mypy app`，19 个源码文件无错误；本次未重复运行 pytest、全仓测试或构建。
- 2026-08-28：按界面反馈移除 CAD 鸟瞰卡片内重复的“鸟瞰图”可见标题，只保留“拖动定位”；可访问名称仍保留完整 CAD 鸟瞰语义，“主体 / 全图”切换不变。仅运行鸟瞰组件专项测试，不重复执行全仓测试、构建、容器重建或文档 reindex。
- 2026-08-28：修复文档 `12cad4b2-064c-4d65-999b-743ab216e5b2`（`5、消控室大样图20200529.dwg`）主体位于左上角却没有“主体 / 全图”切换。正式 v3 bundle 的完整范围为 `[-6729991.29,-273899.87]–[2002282.33,3158349.81]`，9,204 个有界实体中有 468 条关闭图层 `DOTE` 的隐藏 LINE；旧主体样本把不可见实体与可见实体一起统计，1% 裁尾后完整/稳健跨度比仍只有 `1.105`，低于 2 倍门槛，因此 manifest 没有 `focusBounds`，但 ezdxf 实际渲染又跳过这些图元，使可见图组缩在左上角。完整空间索引和 `bounds` 现仍保留所有图元，只有用于动态层级与 `focusBounds` 的确定性样本改为使用 `RenderContext` 判定的实际渲染可见实体，排除关闭/冻结图层及实体自身 `invisible` 标记，避免可见性口径分裂。合成回归同时覆盖 500 个可见主体、250 个关闭图层和 250 个实体级隐藏图元，确认隐藏几何仍扩展完整范围而主体样本保持可见范围。真实 DXF 隔离重建与正式 v4 均生成相同 `focusBounds=[-6645853.74,2990840.24]–[-6417256.22,3066222.55]`、`maxZoom=15` 和 12,032 字节主体缩略图；v4 已以 31/31 分块、原 Embedding 指纹和原 collection 原子激活。登录态页面目视确认首次打开显示清晰主体、“主体”默认选中、“全图”切换和活动状态正确、细节加载完成且无控制台错误。CAD 专项 18/18、Parser 全量 56/56、Ruff、排除既有 Pydantic alias `call-arg` 基线后的目标 Mypy、根级 lint/密钥/文档检查、全仓 typecheck、根脚本 4/4、deployment-agent 3/3、Contracts 30/30、API 266/266（另 1 项按既有条件跳过）、Web 128/128、生产构建、Compose config 与完整拓扑 readiness 通过；未排除该既有基线的目标 Mypy 仍报告 12 条 `PreviewArtifact` / `CadPreviewTileResponse` alias `call-arg`，本次没有新增类型错误。
- 2026-08-28：修复文档 `8837c108-9d27-48ea-8d12-6d81386e3237`（`0）目录、说明、系统图（4#、5#、7#~14#楼、室外）20200610.dwg`）V2 鸟瞰灰白、放大倍率不足及不同 CAD 鸟瞰操作提示不一致。正式 V2 bundle 的快速 `overview-geometry.sqlite` 只有 1 种绘图颜色，而完整索引有 19 种；渐进路径初始化把全图硬编码为灰白，完整索引完成后又只更新存在 `focusBounds` 的主体鸟瞰，因此该正常多图组文件会永久保留灰白全图。其完整/稳健范围比仅 `1.0339`，低于 2 倍主体判定，所以不应伪造“主体”范围；UI 现对 SVG/普通瓦片/渐进瓦片统一显示“鸟瞰图 / 拖动定位”，仅在真实 `focusBounds` 存在时追加“主体 / 全图”。渐进 bundle 在首批细节成功后用 cache-buster 只刷新一次全图与可选主体鸟瞰；完整索引前固定 single-flight，失败后停止自动预取，成功后再恢复 2 并发，避免同文档重复构建超时风暴。Worker 从完整彩色几何原子替换 `overview.png`、z0、可选主体鸟瞰并最后写 `overview-state.json`；缓存瓦片命中但旧 bundle 缺完成标记时也会补齐，异常退出遗留的受控索引临时文件在下一次持锁构建前清理。运行配置 v16 已通过正式发布链路把 `CAD_PREVIEW_MAX_ZOOM` 从旧 runtime 覆盖值 8 更新为 12，并重建 `api`、`parser-worker`、`parser-worker-dwg` 后 readiness 成功；正式 reindex 将目标从 V2 原子激活为 V3，334/334 分块与原 Embedding 指纹保持一致。真实 z12 首次构建进一步证明旧逐线段索引会因曲线/文字轮廓超过 4,000,000 记录失败；完整几何格式升级为只含数值数据的 v2，将连续扁平路径保存为一个 polyline R-Tree 记录，v1 继续只读兼容，仍保留单记录 64 MiB、4,000,000 记录、1 GiB bundle、2 GiB 默认内存与 180 秒上限。相同真实 V3 在约 70 秒内完成：`geometry.sqlite` 125,886,464 字节、188,446 条记录（664 点、105,473 线、21,853 面、60,456 复合路径）、19 种颜色；彩色全图 21,908 字节并含 15 种可见颜色，密集 z12 瓦片实际包含绿/白图元。CAD 专项 17/17、Parser 全量 55/55、Ruff、Mypy 19 个源码文件、根级 lint/密钥/文档检查、全仓 typecheck、根脚本 4/4、deployment-agent 3/3、Contracts 30/30、API 266/266（另 1 项按既有条件跳过）、Web 128/128、生产构建、Compose config 与完整拓扑 readiness 通过。
- 2026-08-27：继续修复 `AGV.dwg` 聚焦主体后放大仍模糊、完整鸟瞰看不到对应图形。根因是完整/主体跨度相差约 8,303 倍：旧 z12 在主体范围只提供约 `229×253` 个源像素，浏览器需铺满约 660 CSS px 且 Retina 还需要 2 倍物理像素，实际仍在放大位图；主体在 218px 全图鸟瞰中只有约 `0.04×0.05px`，不可能肉眼辨认。配置基准继续为 z12，但 manifest 根据完整/稳健主体跨度动态补足并硬封顶 z15；公开/内部瓦片契约、路径校验与 Prometheus zoom label 同步扩展到 0–15，仍只按实际视口生成 PNG。Vue 查看器按 `baseScale × 2^maxZoom / devicePixelRatio` 与当前 focus 适配比例计算真实继续放大上限，最高瓦片达到设备像素密度后禁用“放大”，不再模糊拉伸。Worker 从几何索引额外生成 `focus-overview.png`，普通 bundle 使用完整几何，渐进 bundle 先用受限总览几何并在首次完整索引后原子替换；API 新增同 ACL 的 `/preview/focus-overview`，不返回内部路径。鸟瞰默认“主体”模式并把点击/拖动/方向键映射回完整坐标，可切换“全图”；全图以蓝框标记亚像素主体位置，橙框继续表示当前视窗。正式 reindex 已把 `AGV.dwg` 从 v2 原子激活为 v3，9/9 本机向量回读完成；新 bundle `maxZoom=15`、主体缩略图 101,859 字节、完整几何索引约 108.8 MB、初始 preview 230,496,720 字节。登录态 Chrome 在 `devicePixelRatio=2` 下目视确认 100%/150% 线条与文字清晰，最高 166% 后放大按钮禁用且无加载状态；主体/全图切换、活动按钮文字、蓝色主体框、橙色视窗框均正确。过程中发现 Element 聚焦态把“全图”按钮变成白底白字，已用组件内语义 class 的高优先级 root 覆盖修复。新增 z15 生成/请求/路径/指标、主体缩略图生成与 ACL、主体/全图坐标映射、设备像素缩放封顶和视觉状态回归；CAD 专项 15/15、Parser Ruff/Mypy 19 个源码文件、根级 lint/密钥/文档检查、全仓 typecheck、生产构建、Compose config、根脚本 4/4、deployment-agent 3/3、Contracts 30/30、API 266/266（另 1 项按既有条件跳过）、Web 126/126 与 `git diff --check` 通过。
- 2026-08-27：修复 `AGV.dwg` 浏览器预览首屏一片空白。正式 v1 bundle 有 1,042,218 个图元和 11 种颜色，并非预览未生成；9,433 个可索引实体中约 99% 主体集中在 X `509666–509729`、Y `543918–543986`，但审计生成图层 `$TD_AUDIT_GENERATED_(40BB1)` 上的单个 ARC（handle `44834`，圆心约 `836751.89,1159389.16`）把完整范围拉到约 `34 万 × 62.8 万`，旧查看器按完整范围中点初始化相机而落在空白区。CAD manifest 现增加可选 `focusBounds`：Worker 仅在完整跨度至少为确定性 1% 稳健主体跨度 2 倍时，将带 5% 留白且受完整 bounds 包含校验的主体范围返回；完整 `bounds`、所有图元、瓦片坐标、平移边界和鸟瞰归一化不变。Vue 查看器首次打开与“重置”使用 `focusBounds` 适配主体，旧 manifest 缺字段时保持原行为。正式 reindex 已将 `AGV.dwg` 从 v1 原子激活为 v2，9/9 个本机向量保持原指纹并回读完成；新 manifest 的完整 bounds 为 `[503312.64,537729.54]–[842924.24,1165561.51]`，focusBounds 为 `[509663.65,543914.12]–[509732.10,543989.74]`，`maxZoom=12`。登录态 Chrome 目视确认首次打开直接显示彩色 AGV 主体，鸟瞰可导航到完整范围右上远距区域，从远距区域点击“重置”立即恢复主体。新增 Worker focus 生成/正常范围反例、Pydantic/Zod/OpenAPI 包含关系和 Web 初始瓦片坐标回归；CAD 专项 13/13、Parser Ruff/Mypy 19 个源码文件、根级 lint/密钥/文档检查、全仓 typecheck、生产构建、根脚本 4/4、deployment-agent 3/3、Contracts 29/29、API 265/265（另 1 项按既有条件跳过）、Web 124/124 与 `git diff --check` 通过。
- 2026-08-27：根据 Trace `ca93b97f-338d-4650-8c3e-4e7f0c230168`、`09a22959-bb2c-46c0-9ddb-79989d9dce9a`、`94f1d276-b344-4a2f-8155-4e63a76f4a7a` 与 `3f9d42a2-b5f8-4bac-b6a9-a721269ca4fa` 修复短中文 CAD 文档线索、重复候选挤占和内部文档问题误降级。原实现只对 `#`/楼栋/“图纸”/文件扩展名/稳定英文启用至少 4 字符的文档名匹配，“消控室”和“幼儿园”都只有 3 个连续中文，“消防室”还与图名“消控室”不一致；Rerank 关闭时原始 Top 5 又被相同或近似 CAD 文本占满，导致目标消控室图的“机柜/消防设备/电池柜”分块分别只排在全局第 10 或第 31；grounded LLM 明确资料不足后，`hybrid` 还会生成与项目无关的通用知识。查询端现将“消防室/消防控制室”规范为“消控室”，对带图/室/园等文档特征的问题接受至少 3 个连续中文，仍只在当前 ACL 可见的最多 200 个 active 文档内生成最多 5 个 documentId 白名单。Chroma 在 `QUERY_RECALL_TOP_K` 上限内过取，经 active version 相邻块扩展后折叠规范化正文完全相同的候选，非空 scoped 范围不再用全局近似文档填充，并仍收敛为 `RERANK_TOP_K=5`；不启用 Rerank、不改距离阈值或 ACL。一旦服务端匹配到非空文档范围，零候选、依据不足或引用不可验证都返回 `noAnswer=true/insufficient_relevance`，不再调用 general LLM。API 已重建并 ready；复用历史 Ollama 查询向量的只读回放确认四问分别锁定《5、消控室大样图20200529.dwg》、《3、3#幼儿园弱电平面图20200530.dwg》、同一幼儿园图和目标消控室图，且 `allowGeneralKnowledge=false`；首问只保留目标图中包含“机柜/消防设备/电池柜”和“安保、消控室”的两个上下文，机柜数量问题只保留目标图的四个上下文并包含“UPS柜”与“机柜/消防设备/电池柜”。回放未调用云端 LLM，未新增问答、审计或文档版本。API 专项 30/30、API 全量 265/265（另 1 跳过）、根级 lint/密钥扫描/文档检查、全仓 typecheck、生产构建与 `git diff --check` 通过。现有 CAD 纯文本分块仍可能不足以证明空间尺寸或设备实例数；此时新行为是如实拒答，不猜测数量。
- 2026-08-26：修复 `2、2#教学楼弱电平面图20200530.dwg` 浏览器预览断线、块缺失和颜色全部变灰。根因是复杂图 `formatVersion=2` 路径把 320,210 个模型空间实体按步长 4 抽样，并把 `INSERT`、文字、属性和标注降为单点，最终只保存 398,544 个同色灰白图元；它虽解决总览位图放大模糊，却不能提供 AutoCAD 线条完整性。复杂图现改为 `ezdxf-cad-tiles-progressive`：入库阶段仍在约 25 秒内生成有界快速总览，但将抽样索引隔离为 `overview-geometry.sqlite`，保存不可直接下载且随文档删除的受控 DXF；首次细节请求使用 180 秒 Worker / 190 秒 API 客户端预算，由 ezdxf Frontend 遍历完整模型空间、展开块引用、解析实体/图层颜色并原子发布 `geometry.sqlite`。完整索引设置 4,000,000 图元、1 GiB bundle 与既有内存硬上限，任一上限触发时删除半成品并失败关闭，不再静默缺线。真实文件隔离与正式 v3 bundle 均生成 3,488,840 个图元（150 点、3,474,077 线、14,613 面）、24 种颜色和 377,507,840 字节完整索引，总 bundle 约 555.7 MB；隔离完整构建 134.53 秒。正式 reindex 已将文档从 v2 原子激活为 v3，261/261 个本机向量保持同一指纹并完成回读，页面 warning 更新为 `CAD_PREVIEW_PROGRESSIVE_GEOMETRY`；登录态浏览器在 3,844% 与 8,650% 定位多个楼层平面后确认红、绿、黄、紫等线路、房间块和外围标注连续清晰，加载状态正常结束。新增块展开/颜色、渐进 bundle、完整索引上限失败关闭和 API 首次构建超时回归；CAD 专项 13/13、Parser Ruff、目标 Ruff format、Mypy 19 个源码文件、根级 lint/密钥/文档检查、全仓 typecheck、生产构建、根脚本 4/4、deployment-agent 3/3、Contracts 29/29、API 259/259（另 1 项按既有条件跳过）、Web 123/123 与 `git diff --check` 通过。Parser 原生全量为 48/51，3 项仅因 Docker bind mount 上假转换器失去执行位失败；amd64 全量在 EasyOCR 固定样本进入 QEMU/Torch 后触发 `Illegal instruction`，CAD 专项与本次逻辑无失败，未将两项环境阻塞误记为全绿。
- 2026-08-26：修复 `4、教室大样图教室20200530.dwg` 这类 SVG CAD 连续缩放后逐步跑出可视区。根因是旧 SVG 查看器只修改图片宽度并沿用原滚动偏移，与瓦片查看器的相机坐标语义不一致；按钮缩放现先记录当前视窗中心对应的内容坐标，DOM 尺寸更新后恢复同一中心，Ctrl/Command + 滚轮则保持指针下的图纸点不动，随后统一同步鸟瞰视窗。SVG 继续保留矢量清晰、单资源和低服务端成本优势，大型/复杂图仍走按需瓦片，但两者对用户使用同一套缩放锚点语义。DocumentPreview 专项单测 8/8、Web 全量单测 123/123、目标 ESLint/Stylelint、Web typecheck、生产构建、文档检查和 `git diff --check` 通过。
- 2026-08-26：补齐上传弹窗失败文件的本地删除入口。`upload-file-item.is-failed` 现在在错误行右侧并列显示“重试 / 删除”，删除使用危险色和包含文件名的可访问名称，同时从页面 `uploadRows` 与 Element Upload 的 `selectedUploadFiles` 双向移除，避免再次选文件时旧失败项复活；已排队或上传中的服务端状态不受影响。Pad 400px 弹窗继续保持错误文本与两个操作垂直居中、顺序正确且无横向溢出。DocumentsView 单测 7/7、Web 全量单测 119/119、目标 ESLint/Stylelint、Web typecheck、生产构建、Pad 专项 Playwright 1/1、文档检查和 `git diff --check` 通过。
- 2026-08-26：修复 `2、2#教学楼弱电平面图20200530.dwg` 放大仍无新增细节。目标 DWG 为 38,283,584 字节，ODA DXF 为 135,786,280 字节，解析检查 368,916 个实体并复用 12,603 次块定义；旧 `ezdxf-cad-tiles-simplified` bundle 使用 `formatVersion=1/detailMode=overview`，所有高层级瓦片都只是裁切放大同一张总览 PNG。复杂图快速初始化现以固定种子最多抽样 100,000 个实体、写入最多 500,000 个点/线数值图元，生成 `formatVersion=2/detailMode=geometry` 的非可执行 SQLite/R-Tree；按需瓦片从世界坐标重绘，ASCII OCS 先转 WCS，裸 `INSERT` 不再扩大可见边界，完整 bounds、主要几何稳健范围与动态 `maxZoom<=12` 继续保留。真实目标 bundle 在约 31 秒内生成约 41.8 MB 几何索引，最终 bounds 与 ezdxf 完整索引一致并判定 z12；旧实际 bundle 已在文档锁内原子替换。正式登录态页面和 ACL API 目视确认 z12 高密度瓦片线段/曲线清晰，不再显示模糊总览裁切。两个 Parser Worker 已重建并 healthy；Parser 全量 pytest 49/49、Ruff、Mypy 19 个源码文件、根级 lint/密钥/文档检查、全仓 typecheck、生产构建、根脚本 4/4、deployment-agent 3/3、Contracts 29/29、API 258/258（另 1 项按既有条件跳过）、Web 119/119 与 `git diff --check` 通过。
- 2026-08-25：补齐本地完整拓扑与 DBeaver 的一键启动说明：`pnpm docker:full:db -- up -d --build` 会启动全部常驻 Docker 服务并将 PostgreSQL 映射到 `127.0.0.1:15432`；同时明确 `pnpm docker:up:all` 不加载数据库 GUI 覆盖文件、后续 Compose 子命令需保持同一入口，且 Vue 前端仍需单独启动。
- 2026-08-24：修复 `1、1#教学楼弱电平面图20200530.dwg` 在最大缩放仍无法辨认细节的问题。实际预览 manifest 的完整 X 跨度约 2510 万，而 50,342 个主要实体集中在约 1156 万至 1388 万；原点附近 21 个辅助对象和约 2445 万至 2461 万的 5 个远距对象把总览拉宽，使固定 z8/256× 的最高细节密度仅为正常 `0、目录、说明、系统图（1~3#楼）20200531.dwg` 的约 1/31。瓦片索引现以固定种子的最多 4096 个 bbox 蓄水池样本和两端 1% 稳健范围估计主要几何跨度；完整跨度至少是主要跨度 2 倍时，按二进制倍率动态提高单图 `maxZoom`，硬封顶 12，完整 bounds、远距对象、ACL、按需渲染和缓存边界均保持不变。真实教学楼 DXF 判定为 z12，目录系统图仍为 z8；教学楼 preview bundle 已原子重建为 `maxZoom=12`，最密集区域 z12 瓦片实际生成并目视确认中文字、表格线和签名笔画清晰。两套 Parser Worker 已重建并 healthy。Parser 全量测试 46/46、Ruff、排除既有 Pydantic alias `call-arg` 基线后的 Mypy 19 个源码文件通过；根级 lint/密钥/文档检查、全仓 typecheck、生产构建、目标 Prettier 和 `git diff --check` 通过。
- 2026-08-24：根据 Trace `8652392a-b11d-4546-9670-1c51583a8c69` 修复大型 CAD 重复块遍历放大与可选预览拖过 API 总超时。原始 `重庆S04 AGV项目二期Layout20240806-V5.dwg` 为 28,893,031 字节，ODA 转换后 DXF 约 203,950,529 字节；旧解析器在默认 1,000,000 和诊断最高 2,000,000 实体预算下均触发 `CAD_ENTITY_LIMIT_EXCEEDED`。DXF 文本解析现逐实例保留 `INSERT` 与 `ATTRIB`，但同一 layout、同一块路径的块定义只展开一次；顶层实体、实例属性、嵌套深度、元素数、转换大小和预览渲染资源硬上限保持不变。`cad_summary.metadata` 新增实际检查实体、展开块上下文和复用次数，parserVersion 升为 `nexus-4`。相同真实 DWG 在默认 1,000,000 上限下检查 820,479 个实体、展开 406 个块上下文、复用 5,885 次块定义并输出 926 个知识元素；单独转换加文本解析为 74.1 秒。首次完整路径显示预览初始化在 180 秒后降级、总耗时 287.1 秒，会超过 API 240 秒预算；现对转换后 DXF 不小于 128 MiB、实体预算达到 75% 或块复用达到 1,000 次的大图，将初始预览总预算收敛到最多 60 秒。最终完整临时回归在 169.7 秒返回 926 个元素和 `CAD_PREVIEW_INITIALIZATION_TIMEOUT` 安全 warning，不再让预览失败阻断 RAG。容量错误继续禁止无意义的队列自动重试，但任务 API 现将 CAD 实体、DWG 转换产物和 Parser 元素容量错误标记为可人工重试；管理员完成升级后可复用原文件重排。API 与两个 Parser Worker 均已重建并 healthy；Parser 容器测试 45/45、Ruff、排除既有 Pydantic alias `call-arg` 基线后的 Mypy 19 个源文件、API 文档服务测试 21/21、API lint/typecheck/build 通过。真实诊断未写入业务数据；原失败任务可在页面刷新后点击“重试任务”进入完整入库与向量链路。
- 2026-08-24：Trace `b9970a7a-bd48-4188-b980-aad0aeaaecf2` 证明目标 DWG v3 标题字段已正确入库但仍未进入候选：正式 `AclPolicy → EmbeddingService → QueryRetrievalService → ChromaVectorStore` 路径只返回另一份“目录、说明、系统图”DWG 的 2 个上下文，DeepSeek 因此正确输出资料不足并转 general。根因是目标 ordinal 220 有“建设单位”但没有“9#泉生楼”，而 ordinal 221 的子项目名位于另一 layout；向量正文不含 sourceName。修复增加 `cad_title_summary`，将最多 200 个已配对字段按 layout 汇总并限制为 20,000 字符，parserVersion 升为 `nexus-3`；查询端仅在当前 ACL 可见的最多 200 个 active 文档中接受至少 4 字符的最高文件名线索，追加最多 5 个 documentId 的 scoped 向量查询，VectorStore 再次叠加完整 ACL。完整拓扑已部署并 ready，正式 reindex job `240ef0cc-0450-4281-acd5-0f4de6c26e09` 将目标文档原子激活为 v4，parserVersion `oda-27.1.0.0+ezdxf-1.4.4+nexus-3`，223/223 chunks 完成本机 Embedding 与 Chroma 回读；ordinal 222 汇总同时包含楼栋、建设单位、工程名称、图号、日期和设计编号。相同问题经正式 ACL 服务路径复查，目标 v4 合并上下文以 distance `0.2929846` 进入候选第 3 名，正文明确包含“建设单位：无锡市华夏房地产开发有限公司”。Parser Ruff/Mypy/pytest 43/43、API 目标测试 24/24 通过；根级 lint/密钥/文档检查、全仓 typecheck、生产构建和 `git diff --check` 通过，根脚本 4/4、deployment-agent 3/3、Contracts 29/29、API 256/256（另 1 项按既有条件跳过）、Web 119/119 测试通过。未再次调用云端 LLM；真实回答验证仍需 internal 片段出网授权。
- 2026-08-24：根据 Trace `33ff463e-7f85-450b-a41f-d3f0a3d9a98d`、`16a53ad3-a655-4ab3-bbb1-1bd8c9725b7f` 与 `fa850550-5071-4bed-877b-760c21686afe` 修复两类检索语义缺陷。前两条“材料列个表格/所需材料列个表格”均未加载上一问，第二条仅因“所需材料”独立语义更强而命中《西班牙NIE申请教程.pdf》第 13 页；上下文检测现覆盖对象在前的短续问并保留明确主题反例。目标 DWG 原 active v2 共 220 chunks，解析元素中“建设单位”位于 `[682213.529,15603.077]`，“无锡市华夏房地产开发有限公司”位于同一行右侧 `[684421.168,15552.510]`，但原线性分块分别落在 ordinal 159/164，邻居窗口 1 无法恢复关系。DXF parser 现对白名单标题字段在同一 layout/块坐标系内生成“字段：右侧同行最近值”的 `cad_title_field`，保留坐标/handle 并将 parserVersion 升为 `ezdxf版本+nexus-2`；不扩大全局邻居窗口。完整 DWG 拓扑已重建并 ready；正式 reindex job `519eec69-a691-4b02-842d-96f5c4cfe80b` 将目标文档原子激活为 v3，parserVersion `oda-27.1.0.0+ezdxf-1.4.4+nexus-2`，222/222 chunks 完成本机 Embedding 与 Chroma 回读。新 ordinal 220 单块包含“建设单位：无锡市华夏房地产开发有限公司”、工程名称、图号、日期和设计编号。Ruff、Parser pytest 43/43、排除仓库既有 Pydantic alias `call-arg` 基线后的 Mypy 19 个源文件通过；根级 lint/密钥/文档检查、全仓 typecheck、生产构建和 `git diff --check` 通过，根脚本 4/4、deployment-agent 3/3、Contracts 29/29、API 254/254（另 1 项按既有条件跳过）、Web 119/119 测试通过。完整 Mypy 不排除 alias 基线时仍有 44 个跨 10 个未改/已改模块的既有 `call-arg` 错误；真实 LLM 问答验证需要单独批准 internal 片段出网，当前未执行。
- 2026-08-24：完成第二轮问答性能优化。近 30 天 350 条 QueryAudit 的 P50/P95/最大值为约 `3306/24396/138439 ms`；grounded 154 条 P95 `62253 ms`，general 88 条 P95 `67327 ms`。Provider 用量事实完整区间为 2026-08-11 15:57 UTC 起的 51 条查询，其中 26 条 `LLM_ANSWER_UNVERIFIABLE` 均执行 3 次 LLM，P50/P95 为 `7424/19100 ms`；15 条单次 LLM 的 P50/P95 为 `5982/12627 ms`，9 条无 LLM 请求 P95 `2475 ms`，确认引用修复/通用降级是主要尾延迟来源。Ollama 合成探针显示原 `/v1/embeddings` 为 `2139 ms`，原生 `/api/embed` 首次 `206 ms`、后续 `190–192 ms`；两者 1024 维向量 cosine=1、逐维最大差值 0。实现已切换原生端点并加入 60 秒至 24 小时的 `OLLAMA_KEEP_ALIVE`、关闭静默截断和记录内部/加载耗时；grounded 明确“资料不足”时直接进入安全 general/no-answer 分支，不再浪费一次引用修复，缺失/越界引用仍修复一次。新增 Embedding 缓存命中与引用事件指标，用量契约/页面增加 P50。根级 lint/密钥/文档检查、全仓 typecheck、生产构建和 `git diff --check` 通过；根脚本 4/4、deployment-agent 3/3、Contracts 29/29、API 253/253（另 1 项按既有条件跳过）、Web 119/119 测试通过。API 已使用 `--no-deps` 单独重建且 readiness 全部为 up；部署后 5 次无敏感本机原生 Embedding 探针 P50/P95 为 `175/352 ms`，后四次为 `173–185 ms`。使用真实管理员会话复测同题会把已授权知识片段发送给云端 LLM 并产生费用/审计，当前执行环境未获得该项单独数据出网授权，因此未执行；不得把组件级结果冒充完整问答 P50/P95 或质量结论。
- 2026-08-24：根据 Trace `3d22b98f-c8ae-4a86-887a-c796d17b12ac` 修复省略主语的连续问答漏判：原会话“西班牙的 NIE 申请条件 → 列个需要的材料表格”第二轮未命中显式代词规则，虽召回 5 个知识片段，但两次 grounded 输出均未通过引用校验，最终以 `LLM_ANSWER_UNVERIFIABLE` 降级为通用知识补充。上下文检测现同时覆盖显式指代和有界的短动作型续问，仍排除“列出 PostgreSQL 锁类型表格”“总结西班牙 NIE 申请条件”等带明确主题的独立问题；服务回归确认检索、Rerank 和 LLM 均收到安全标记的上一用户问题，历史助手答案与来源正文不重新出网。根级 lint/密钥/文档检查、全仓 typecheck、生产构建和 `git diff --check` 通过；根脚本 4/4、deployment-agent 3/3、Contracts 29/29、API 249/249（另 1 项按既有条件跳过）、Web 119/119 测试通过。已重建本地 API；Compose 同步重建其 Parser 依赖，API、Parser、PostgreSQL、Redis 与 Chroma 均为 healthy，`/health/ready` 全部检查为 up。
- 2026-08-24：完成问答热路径基础优化：Rerank 关闭时 Chroma 直接按最终 Top 5 召回，不再让额外 15 个候选进入 PostgreSQL 相邻块扩展；不含指代的独立续问在 Provider 前仅校验 tenant + user 会话归属，不读取历史轮次；Query Embedding 缓存命中不再每次清理过期行或写滑动 TTL，只在未命中时并行清理、剩余 TTL 进入后半程时续期。新增问答分阶段 Prometheus Histogram，可分别观察限流、历史、Embedding、检索、Rerank、两次授权、LLM、审计和历史写入耗时；保留引用校验、返回前授权复核和审计失败关闭。根级 lint/密钥/文档检查、全仓 typecheck、生产构建和 `git diff --check` 通过；根脚本 4/4、deployment-agent 3/3、Contracts 29/29、API 246/246（另 1 项按既有条件跳过）、Web 119/119 测试通过。
- 2026-08-24：重做 Mobile 全类型文档预览工具栏：文件名/实时权限标记位于左侧信息区，全屏固定右上；PDF、图片、Office 转 PDF与文本/Markdown 不再渲染巨大空操作区，CAD 才在下方追加“缩小 / 当前比例 / 放大”三列等宽缩放行。引用位置存在时单独占行，交互错误在 Mobile 恢复正常流，长文件名为右上全屏保留安全宽度并自然换行，工具栏无水平溢出。浏览器回归扩展为 425px 下 CAD、原生 PDF、PNG、DOCX 转 PDF 和 Markdown 五种 manifest，校验右上全屏对齐、非 CAD 紧凑高度、CAD 三列等宽与容器无溢出。根级 lint/密钥与文档检查、Web typecheck、Web 单测 119/119、DocumentPreview 专项单测 6/6、生产构建、`git diff --check` 和五类预览 Playwright 1/1 通过。
- 2026-08-24：修复文档移动操作面板的预览行右侧箭头错位：将原会同时命中 RouterLink 直接文字/箭头 span 的 `:deep(> span)` 收紧为 `.el-button.mobile-action-item :deep(> span)`，只展开 Element Button 内部 wrapper，四行 chevron 恢复同一右边界。同时完成生产 Vue/Markdown 的 ARIA role 审计：移除文档操作面板未实现完整方向键交互模型的 `menu/menuitem`；保留异步错误 `alert`、非紧急状态 `status/aria-live`、div 标题 `heading + aria-level`、受控 Markdown list/region 以及具备完整键盘行为的任务折叠 button role。根级 lint/密钥与文档检查、Web typecheck、Web 单测 119/119、生产构建、`git diff --check`、`menu/menuitem` 零遗留扫描和相关 Playwright 1/1 通过。
- 2026-08-24：删除问答历史与部门目录中仅供旧测试定位、不再承担样式或行为职责的 `history-list-panel`、`history-list-scroll` 和 `department-directory__list`；生产模板只保留 `kb-block-content`、`kb-block-scroll`、`kb-block-scroll--list` 及实际行交互 class。组件测试与 E2E 改用列表 `aria-label`、直接滚动子节点和部门 `nav` 结构定位；`history-layout`、`history-list-row/item` 与 `department-list-item` 仍有布局、状态或行为职责，保留。根级 lint/密钥与文档检查、Web typecheck、Web 单测 119/119、生产构建、`git diff --check`、HistoryView 专项单测 4/4、相关 Playwright 3/3 和三类 class 零遗留扫描通过。
- 2026-08-24：问答历史与 PC / Pad 部门目录列表统一复用新的 `kb-block-scroll--list`，在 `kb-block-scroll` 基础上集中内容起始对齐、overscroll、列表内距与稳定 scrollbar gutter，删除两页重复滚动声明。部门整行入口由 `el-button` 改为不声明 button role / `aria-pressed` 的 `div`，保留 `tabindex="0"` 与 Enter/Space 选中，部门名改为 `span + kb-text*`，同时删除失效的 Element Button wrapper `:deep()` 规则。历史会话行同步移除 `role="button"`、`role="listitem"` 与 `aria-pressed`，保留键盘操作。根级 lint/密钥与文档检查、Web typecheck、Web 单测 119/119、生产构建、`git diff --check`、HistoryView 专项单测 4/4 和相关 Playwright 2/2 通过。
- 2026-08-24：修复水平字段行的 label 换行：`kb-data-field__label` 从与 collapse label 共用的可收缩 Flex 规则中分离，改为 `flex: 0 0 auto` 和 `white-space: nowrap`，以内容宽度固定单行；`kb-data-field` 同步使用顶部对齐，避免 label 元素被右侧多行 value 拉伸。value 继续承担剩余宽度的收缩与安全换行。补充桌面和 375px Mobile 下 label `nowrap` 与单行实际高度回归。根级 lint/密钥与文档检查、Web typecheck、Web 单测 119/119、生产构建、`git diff --check` 和相关 Playwright 1/1 通过。
- 2026-08-24：在保留用户将问答历史整行入口改为 `div` 的基础上收口 `history-list`：列表面板直接复用 `kb-block-content`，真实滚动节点叠加 `kb-block-scroll`，移除只重复 Flex/overflow 的 `history-list-card` / `history-list` 中间层与失效的 Element Button `:deep(> span)` 规则。可点击 `div` 补齐 `role="button"`、`tabindex="0"`、`aria-pressed` 与 Enter/Space 操作；列表标题、错误标题与问题角色均从 `strong` 改为 `span + kb-text*`，仅保留标题/摘要单行省略所需的页面 class。根级 lint/密钥与文档检查、Web typecheck、Web 单测 119/119、生产构建、`git diff --check`、HistoryView 专项单测 4/4 和相关 Playwright 2/2 通过。
- 2026-08-24：延续数据文字与 Element Button 模板精简：`kb-data-grid__value` 收回主文字色、正文字号、紧凑行高和中等字重，审计、入库、系统与用量网格的 18 个值节点不再重复组合 `kb-text*`；根据本地 Element Plus 2.14.3 类型声明确认 `nativeType` 默认为 `button`，删除 Web 模板全部冗余 `native-type="button"`，只保留表单实际需要的 `native-type="submit"`。根级 lint/密钥与文档检查、Web typecheck、Web 单测 119/119、生产构建、`git diff --check`、两项零遗留扫描和相关 Playwright 5/5 通过。
- 2026-08-24：完成 Web 按钮与水平字段行收口：文档移动操作面板、应用壳层、部门目录、历史列表和问答/历史来源卡的 Vue 交互统一使用 `el-button`，业务 Vue 模板不再包含原生 `<button>`；仅 Markdown 经清洗后由 `v-html` 渲染的行内来源保留 1 处原生 button，以维持原生键盘语义和委托事件。`kb-data-field__label/value` 收回稳定字号、行高和主/次文字色，删除 43 组模板重复文字 class，只保留角色值加粗 modifier。高风险 `document-dialog-body` 以 Grid `gap` 统一子项间距，并补齐 Element Button 内部 wrapper 与整卡自适应高度兼容。根级 lint/密钥与文档检查、Web typecheck、Web 单测 119/119、生产构建、`git diff --check` 和相关 Playwright 8/8 通过。
- 2026-08-24：移动端文档“修改权限”由右侧全屏 Drawer 改为内容自适应高度的底部 Drawer；用户目录“新增账号”、“管理后台账号”与“编辑托管角色”保留 PC / Pad Dialog，Mobile 统一切换为底部 Drawer；`el-message-box` 圆角与 `el-dialog` 共用 `--kb-radius-md`。补充 Drawer 方向、底部贴合、高度边界与 MessageBox 圆角的 Playwright 回归。Prettier、ESLint、Stylelint、Web typecheck、Web 单测 119/119、生产构建、文档检查、`git diff --check` 和相关 Playwright 2/2 通过。
- 2026-08-24：完成 Vue 静态 class 与样式选择器二次审计，删除 42 个仅用于旧测试定位、对称命名或已失去样式/行为职责的 class，包括 `access-filter-search`、`access-filter-department`、`access-create-action`、页面私有数据网格 alias、无样式标题/标签 class 与 Drawer 定位 class；`history-toolbar--mobile` / `usage-toolbar--mobile` 改为有独立样式职责的语义 class。单元测试和 E2E 改用共享结构、`aria-label`、可访问角色或控件关系定位；生产 Vue 静态 class 无未引用候选，业务 CSS/SCSS 无孤立选择器。Prettier、ESLint、Stylelint、Web typecheck、文档检查、Web 单测 119/119、生产构建和完整 `phase15.spec.ts` Playwright 39/39 通过。
- 2026-08-24：全站文字标签与样式收口：业务 Vue 中普通展示文字不再使用 `el-text`，按当前产品约束统一为 `div` / `span`（不使用 `p` 或 `h1`–`h6`），标题继续通过 `role="heading"` / `aria-level` 保留语义。`main.css` 建立完整 `kb-heading + h1…h6` 与正交的 `kb-text` 尺寸、颜色、字重体系；字段/数据网格、标题、说明、状态与错误文字组合公共 class，删除旧 `kb-caption` 及可由公共体系表达的 scoped 字号、颜色和字重，Markdown、登录 Hero、导航交互、代码/指纹和响应式特殊效果保留局部规则。Prettier、ESLint、Stylelint、Web typecheck、文档检查、`git diff --check`、Web 单测 119/119、生产构建和完整 `phase15.spec.ts` Playwright 39/39 通过。
- 2026-08-24：`--kb-block-padding` 恢复为全端统一的 `var(--kb-space-3)`（12px），删除 Pad/Mobile 中与根级相同的重复覆盖，继续保持区块间距与容器内距语义分离，并与前端设计规范的三端 12px 面板基线一致。目标 Prettier、Stylelint、`git diff --check` 与生产构建通过；相关 Playwright 2/3，通过 Mobile 全页面与滚动模型，剩余共享数据网格用例被系统摘要值当前缺少 `kb-data-grid__value` 阻塞，与本次 padding 修改无关。
- 2026-08-23：全面整理 `main.css` 公共 Class 与全部 Vue、动态 Markdown、单元测试和 E2E 引用：统一为 `kb-*` 命名空间与 BEM，页面、页头、块标题、筛选、回答来源、品牌和状态页不再保留旧名；可独立复用的标题组/操作组不强行作为页头 Element，公共 modifier 均与基础 Block 配对。删除 3 个未使用文本规则、无效果的 Mobile modifier 和注释残留，抽取 `kb-control-toolbar` / `kb-caption`，并补齐系统摘要值复用、Mobile 数据网格两列和标题语义。目标 Prettier、ESLint、Stylelint、Web typecheck、文档检查、`git diff --check`、Web 单测 119/119、生产构建和完整 `phase15.spec.ts` Playwright 39/39 通过；浏览器回归中曾出现 Vite 动态导入 fetch 抖动，单用例重跑 1/1、最终完整回归无失败。
- 2026-08-23：用量 `usage-summary` 与系统 `system-overview` 统一为三列零内距 `kb-data-grid`，并分别使用对称的 `usage-summary__item/label/value` 与 `system-overview__item/label/value` 命名；三个子项各自挂载 `kb-block` border，Mobile 收为两列。Usage 强调值、总体状态 success/warning 色、`component-card` 两列结构与 `task-data-list` / `queue-data-list` 零 padding 均保留。目标 Prettier、ESLint、Stylelint、Web typecheck、文档检查、`git diff --check`、生产构建、Usage/入库组件测试 6/6 和共享数据网格 Playwright 1/1 通过。
- 2026-08-23：审计表格展开的 `audit-details` 与入库任务展开的 `task-data-list` 收敛为 `main.css` 共享 `kb-data-grid`；统一布局、内距、字段间距、次要标签和 600 字重值，按用户要求不添加 border、border-radius 或 background。审计使用三列、任务使用四列，Mobile 统一两列；删除两个页面原有的重复 Grid/字段 CSS。目标 Prettier、ESLint、Stylelint、Web typecheck、文档检查、`git diff --check`、生产构建、入库组件测试 1/1 和共享数据网格 Playwright 1/1 通过；Playwright 结束时 Vite 记录了既有动态导入 fetch warning，未影响本用例断言。
- 2026-08-23：Mobile 不再为“返回文档列表/详情/会话列表”单独渲染第二行 `el-page-header__header`；返回入口改为固定 `app-header` 中汉堡按钮与当前页标题之间的 ArrowLeft 图标，使用 `aria-label` / `title` 保留完整返回目标，页标题支持单行省略。PC / Pad 继续使用既有 `el-page-header`。目标 Prettier、ESLint、Stylelint、Web typecheck、文档检查、`git diff --check`、生产构建和文档/历史 Mobile 返回导航 Playwright 2/2 通过。
- 2026-08-23：文档详情 PC / Pad 将 `detail-actions kb-block kb-status-toolbar` 移为 `document-detail-page` 直接顶部行，与分块页 `chunks-toolbar kb-block` 保持一致；下方多区块收入 `detail-content kb-page__content` 独立滚动，操作栏不随其移动。Mobile 继续由整个详情页滚动，标题操作卡可随页面滚走。目标 Prettier、ESLint、Stylelint、Web typecheck、文档检查、`git diff --check`、生产构建和 PC / Pad 固定操作栏 Playwright 1/1 通过。
- 2026-08-23：Mobile 文档分块页从“固定 `chunks-toolbar` + 剩余高度内部滚动”改为整个 `document-chunks-page` 纵向滚动；工具栏可随页面滚走，`kb-block-content` / `kb-block-scroll` 改为自然高度，避免列表被压缩在过小的可视区。Mobile 版本选择器同步改为满宽，摘要与 fingerprint 左对齐；PC / Pad 滚动模型不变。目标 Prettier、ESLint、Stylelint、Web typecheck、文档检查、`git diff --check`、生产构建和独立 Mobile 分块滚动 Playwright 1/1 通过；分块组件测试 2/3，唯一失败是工作区当前模板已移除标题 `role="heading" aria-level="2"`、测试仍要求该语义的既有漂移，与本次滚动布局无关。
- 2026-08-23：检查 Web 全部 Vue 的 7 个 `el-link`，统一挂载 `kb-link` 并将文字收入 `kb-link__text`；`element.css` 集中覆盖 Element Plus 外层与 `.el-link__inner` 默认居中对齐，`main.css` 集中提供安全收缩和单行省略。入库文件名使用 `kb-link--fill` 在 Mobile 从左侧开始，文档 ID 使用 `kb-link--end` 保持右对齐，其他文字链接保持自然宽度。目标 Prettier、ESLint、Stylelint、Web typecheck、文档检查、`git diff --check`、生产构建、相关组件测试 8/8 和 Mobile 超长任务名 Playwright 1/1 通过；Playwright 结束时 Vite 记录了既有动态导入 fetch warning，未影响本用例断言。
- 2026-08-22：入库任务文件名、文档表格/移动卡片标题、详情页文档 ID、“查看全部”与最近任务等文字型可点击入口统一改为 `<el-link type="primary" underline="never">`；内部路由使用 `RouterLink custom` 传递 `href` / `navigate`，保留 SPA 导航和标准锚点能力，同时避免嵌套 `<a>`。按钮、品牌、侧栏、整行/整卡和移动菜单项继续保留原语义。目标 Prettier、ESLint、Stylelint、Web typecheck、文档检查、`git diff --check`、生产构建、相关组件测试 8/8 和 375px 全页面 Playwright 1/1 通过。
- 2026-08-22：文档详情页根布局、标题操作栏、标题/操作区、危险与 warning 文本、最近任务字段行改为优先复用 `main.css` 的 `kb-page`、`kb-status-toolbar`、`kb-title-group`、`kb-action-group`、`kb-block__title`、`kb-text--*` 和 `kb-data-*`；删除对应重复 Flex、宽度、间距、颜色与分隔线声明，保留双卡 Grid、DWG 说明、复制交互和移动操作面板等页面专属样式。目标 Prettier、ESLint、Stylelint、Web typecheck、文档检查、`git diff --check` 与生产构建通过；详情三端 Playwright 综合用例在到达文档详情断言前，被工作区既有的“用户与角色”heading 语义断言阻塞，未误记为通过。
- 2026-08-22：文档预览页移除仅为降级分块添加边框、不承担定位或交互的 `is-referenced` 状态；工具栏、页头内容/操作、降级列表间距与滚动区改为优先复用 `main.css` 的 `kb-status-toolbar`、`kb-title-group`、`kb-action-group`、`kb-block-content*`、`kb-block-scroll` 和 `kb-text--warning`，仅保留媒介预览、CAD 交互与页面特有视觉。目标 Prettier、ESLint、Stylelint、Web typecheck 和文档预览组件测试 6/6 通过。
- 2026-08-21：问答输入区升级为图标发送、作用域提示、接近上限时显示字数与提交中的旋转状态；`AskComposer` 在提交期间同步禁用文本输入和发送操作，避免请求完成后清空用户等待期间输入的内容。新增仅供该输入区使用的 `--kb-shadow-input`，使其以可见阴影从页面背景中托起，聚焦时继续使用主色边框。AskComposer 组件测试 3/3、目标 ESLint/Stylelint、Web typecheck、Prettier、`git diff --check` 与真实提交流程 Playwright 1/1 通过；独立问答页头用例仍在既有的“从资料中找到答案”heading 语义断言处失败，未将其记为通过。
- 2026-08-21：修复文档管理、审计中心和用户与角色 Desktop Table 的双底边观感：无内距边框块关闭 Element Table 底部伪元素、固定列底部补丁及最后一行单元格底边，仅由外层 `kb-block` 绘制终止线，其他行分隔线不变。三张管理表格浏览器回归确认外层底边为 1px、内部终止线均为 0 或不渲染。
- 2026-08-21：用量与成本筛选从可编辑的日期时间控件收敛为三端统一的只读、日粒度单面板日期范围，控件直接位于工具栏并移除 Mobile 筛选 Drawer；PC/Pad 对齐 `audit-filter-form` 使用紧凑 Flex 排列和 350px daterange 标准宽度，Mobile 填满重置按钮之外的可用空间，浮层与触发器等宽并左侧对齐。三端单面板根节点、body-wrapper、body 与 content 均为 Popper 的 100% 宽度，清除内部默认 322px 最小宽度并以 content padding 保留日历留白。各端均不允许键盘输入，查询边界按本地自然日规范化，开始日期为 00:00，结束日期覆盖至 23:59:59.999。Web 单测 118/118、375px 综合、425px Mobile 日期浮层、横屏 Mobile 与三端工具栏 Playwright 4/4、Web ESLint/typecheck、Stylelint、文档检查、`git diff --check` 与生产构建通过。
- 2026-08-21：修复 Mobile 聚焦输入控件时 iOS 自动放大页面：新增 `--kb-font-size-mobile-input: 16px` 语义 token，并统一应用于 Element Plus Input、Textarea、Select、DatePicker/Range 输入文本；继续保留 viewport 用户缩放能力，不使用 `user-scalable=no` 规避问题。375px Playwright 同步覆盖问答 textarea、文档搜索与用量日期输入的 16px 计算字号。目标 Prettier、ESLint、Stylelint、Web typecheck、文档检查、`git diff --check` 与 375px 综合 Playwright 1/1 通过。
- 2026-08-21：修复 Mobile 用量 DatePicker 浮层内容偏左与英文内置文案：高优先级全局原生选择器覆盖 Element Plus 动态加载的 322px 面板定宽，使日期面板填满安全视口 popper，并让日历内容保持左右对称 token 内距；根级 ConfigProvider 实际传入简体中文 locale，年月、星期、“此刻”和“确定”统一中文。前端类型检查、全量 Web 单测 117/117、目标 Prettier 与独立 375px Playwright 回归 1/1 通过；原 375px 综合用例已通过本次日期浮层断言，后续被既有文档 ID 复制入口可访问名断言阻塞，未误记为全绿。
- 2026-08-20：完成全量 27 个 Vue SFC 与前端测试一致性审计，集中同步 `*.test.ts` 和 `phase15.spec.ts` 中已漂移的可访问名、共享 class、断点、工具栏高度、Drawer 尺寸、分页背景、滚动目标、空态和 Element Plus 结构断言。来源抽屉移除失效的 `heading/panel/text-block/:global()`，改用 `kb-*` 语义结构、token 与行内安全高度；文档 ID 文本本身作为点击复制触发区，不额外显示“复制”按钮，详情/分块/来源标题补齐 heading 语义，viewport 恢复用户缩放。清理 `DocumentDetailView` 空 Desktop media query，并将误复制为第二套文档详情实现的 `ComingSoonView` 恢复为轻量占位页，避免平行业务页面继续漂移。全量 Web 单测 117/117、`phase15.spec.ts` 35/35、Prettier、ESLint、Stylelint、Web typecheck、文档检查、`git diff --check` 与生产构建通过。
- 2026-08-20：全站页码分页位置统一收敛到对应 `kb-block-content` 内容外壳底部：文档管理、审计中心和入库任务不再把分页作为 `kb-page` 直接子节点，用户目录与文档分块保持既有正确结构；带边框的 `kb-block + kb-block-scroll` 与透明分页统一为同级，分页不再进入 border 块。文档预览降级列表同步拆分正文滚动区与固定底部分页；组件测试补齐共享外壳及非边框父级断言，分块组件与 E2E 定位同步移除已失效的 `document-pagination` 旧类名。目标 Prettier、ESLint、Stylelint、Web typecheck、文档检查、`git diff --check`、生产构建、分页相关组件测试 19/19 和管理页滚动模型 Playwright 1/1 通过；入库工具栏综合用例在到达本次边框/分页断言前被既有高度旧断言阻塞（预期 74px、当前实际 40px），未误记为通过。
- 2026-08-20：修复用户目录与部门权限内容块无法稳定纵向滚动：`access-table-wrap` 移除无布局职责且会引入 Grid 内容最小尺寸的 `display: grid`，继续由 `kb-block-scroll` 承担滚动；Mobile `department-layout` 显式挂载 `kb-block-scroll` 并改为普通 Block，避免唯一 Collapse 被 Grid 拉伸后由 `overflow: hidden` 裁掉；Desktop/Pad 仍由左右子面板各自滚动。目标 Prettier、ESLint、Stylelint、Web typecheck、文档检查、`git diff --check`、生产构建、430px 用户目录回归和新增 667px Mobile 双容器滚动 Playwright 1/1 通过。既有 852px 综合用例已通过用户目录与部门内部面板滚动断言，后续仍被 Pad 视口查找 Mobile 菜单的旧断言阻塞，未误记为全绿。
- 2026-08-20：文档分块移除无样式、脚本或独立测试职责的 `chunks-content` class，`phase15.spec.ts` 改用 `.document-chunks-page > .kb-block-content` 共享结构定位。首轮浏览器验证发现分页被拆成 Grid 第三个直接子节点后下移到内容边界之外，已将分页放回共享 `kb-block-content` 外壳，保留两行 Grid 与内部滚动模型。目标 Prettier、ESLint、Stylelint、Web typecheck、文档检查、`git diff --check`、生产构建、分块组件测试 3/3 和管理页滚动模型 Playwright 1/1 通过。375px 全页面综合用例已通过分块分页边界断言，后续仍被既有用量日期浮层“确定”文案断言阻塞，未误记为全绿。
- 2026-08-20：主导航新增路由级 `activeNavigation` 归属，不再仅依赖 Vue Router 对当前路由记录的默认 active 判定；文档详情、预览和分块统一保持“文档管理”选中，PC、Pad 与 Mobile sidebar 共用 `is-active` 视觉状态和 `aria-current="page"` 语义。目标 Prettier、ESLint、Stylelint、Web typecheck、文档检查、`git diff --check`、生产构建、路由归属单测 13/13 与文档详情 PC/Pad/Mobile Playwright 1/1 通过。误执行的全量 Web 单测为 114/115，唯一失败仍是既有 Provider Anchor `40` / `36` 高度断言，未误记为全绿。
- 2026-08-20：应用壳层与问答工作台的 `kb-page-header` 改为 Element Plus `el-page-header`，标题说明与操作分别使用 `content` / `extra` 插槽，保留右侧返回入口、Mobile 结构分支和页面滚动边界；页面一级标题同步补齐 heading 语义。目标 ESLint、Stylelint、Web typecheck、文档检查、`git diff --check`、生产构建和问答页/壳层 Pad/Mobile Playwright 3/3 通过；既有问答综合用例仍被 `AskComposer` 当前“输入问题”可访问名与用例旧的“输入知识库问题”断言不一致阻塞，未误记为通过。
- 2026-08-20：应用顶栏 `top-breadcrumb` 由手写文本与分隔符改为 Element Plus `el-breadcrumb` / `el-breadcrumb-item`，保留“页面分组 / 当前页面”内容、Pad 展示与 Mobile 隐藏规则。目标 ESLint、Stylelint、Web typecheck、文档检查、`git diff --check`、生产构建和独立 Pad 面包屑 Playwright 1/1 通过；既有 768px 壳层综合用例仍在面包屑断言前被页面标题缺少 heading 语义的已有断言阻塞，未误记为通过。
- 2026-08-20：全站页面间距收敛到语义 token：页面/组件区块优先使用 `--kb-layout-gap`，紧凑列表行使用 `--kb-list-row-padding`，块级容器与工具栏使用 12px 的 `--kb-block-padding`；业务 Vue/CSS 已无直接 `var(--kb-space-3)`，仅 `tokens.css` 保留五处语义变量定义。Web Stylelint、ESLint、文档检查、`git diff --check` 与独立 Vite 生产构建通过。全量 Web typecheck 仍被 `SourceDrawer.vue` 引用已删除的 `isPhone` 阻塞；全量单测 100/102，失败为对应 SourceDrawer 移动抽屉断言和既有 Provider Anchor 高度断言；375px 全页面 Playwright 专项仍被既有 Vite 动态导入 `DocumentsView.vue` 失败阻塞，均未误记为通过。
- 2026-08-19：收口短视口 Dialog 与 Pad sidebar：全局 Element Dialog 改为纵向 Flex，header/footer 固定且 body 独立滚动，上传文档、新增账号、管理后台账号在 360px 高度下不越界；Pad 图标导航移除 `tablet-group-title` / `tablet-group-flyout` 与点击打开状态，hover/键盘聚焦只显示当前入口 Tooltip，点击直接导航，`app-sidebar` 独立支持 Y 轴滚动。目标 ESLint、Stylelint、Web typecheck、Mobile/Pad/短视口 Playwright 3/3、生产构建与文档检查通过。
- 2026-08-19：用户目录 `access-toolbar` 在 Pad 端取消两行 Grid 定位，与 PC 一致使用“搜索 + 部门 + 筛选/重置 + 新增账号”单行布局；768px 回归新增同行、74px 高与无横向溢出断言。目标 ESLint、Stylelint、Web typecheck、三端工具栏 Playwright 1/1、生产构建与文档检查通过。
- 2026-08-19：全站 opacity 收敛为 `tokens.css` 的 hidden、decorative、disabled、muted、visible 五档；原 0.55/0.65 合并为 muted 0.6，历史删除入口、文档反馈、问答脉冲、移动禁用操作和状态页装饰均改用语义 token。Stylelint 新增规则禁止 CSS/Vue 再写裸 opacity 数字，全仓扫描无遗留。
- 2026-08-19：完成历史、文档与移动筛选细节回归：PC/Pad 问答历史每次搜索后清空右侧旧会话与 URL `conversationId`；PC/Pad 文档管理空数据时不再渲染空 Table，统一使用带图形的 `el-empty`；文档详情基本信息新增完整 UUID 与复制入口，入库任务继续按 UUID 精确筛选。Mobile `access-table-wrap` / `department-layout` 保留外层单边框并移除内层 Collapse 边框；文档、入库与用户筛选按钮统一按 Drawer 非空条件显示 `kb-filter-trigger--active::after` 激活点。目标组件测试 15/15、历史/文档/文档详情/用户部门/入库筛选浏览器专项 5/5、根级 lint/密钥扫描/文档检查、Stylelint、Web ESLint/typecheck、E2E TypeScript 与生产构建通过；全量 Web 单测 100/102，仍仅为 Provider Anchor 两项既有断言失败。
- 2026-08-19：修复移动筛选状态与列表 surface 回归：文档、入库、用户及用量 Drawer 的“重置”改为清空本页全部筛选并立即重新加载，不再遗留工具栏 Input/Select；入库文档 ID 在 API 请求前校验 UUID，非法值只显示字段错误，字段/API 错误态不显示旧分页。Mobile 问答历史移除“筛选”按钮并将空态改为剩余空间双轴居中；`access-table-wrap` 与 `department-layout` 恢复白色 `kb-block` surface。目标组件测试 13/13、移动布局/文档重置/用户重置/入库校验与重置浏览器专项 4/4、根级 lint/密钥扫描/文档检查、Stylelint、Web ESLint/typecheck、E2E TypeScript 与生产构建通过；全量 Web 单测 98/100，仍仅为 Provider Anchor 两项既有断言失败。
- 2026-08-19：完成移动筛选与 Element Plus 浮层收口：审计中心在 Mobile 将事件类型 Select/重置直接收入 `audit-toolbar` 并移除筛选 Drawer；移动筛选触发按钮统一 `kb-filter-trigger`，历史与入库搜索 Input 移除前置 Search 图标。根级 Element ConfigProvider 启用简体中文 locale，Select/DatePicker/Dropdown 等浮层统一当前 UI 的 surface、边框、圆角、阴影与交互色；用量 Drawer 内 DatePicker 改为 Teleport 至高层级全局浮层，避免被 Drawer 滚动容器裁剪。目标组件测试 8/8、375px 浏览器专项 1/1、根级 lint/密钥扫描/文档检查、Stylelint、Web ESLint/typecheck 与生产构建通过；全量 Web 单测 96/98，仍仅为 Provider Anchor 两项既有断言失败。
- 2026-08-19：对齐上传 Drawer、Mobile sidebar 和上传 Dialog 的 Close 交互：移动侧栏 Close 改为原生按钮，三者统一 default/hover/focus/active 色阶；边框全程保持中性色，hover/focus 仅使用 primary-soft 背景，active 使用 nav-accent 背景，不再出现蓝色边框。
- 2026-08-19：收口全站错误态与弹层 Close 视觉：修正“无法加载入库任务”标题语义，`kb-error-state` 统一使用 danger-soft 背景、边框、lg 圆角和 600 标题字重，不依赖外层是否为 `kb-block`；Dialog/Drawer 默认 Close 及 Mobile sidebar 自定义 Close 统一为带边框的圆形控件。
- 2026-08-19：优化 Mobile sidebar 顶部身份展示：品牌与关闭按钮保持首行，用户头像、用户名和部门收入独立的 `mobile-sidebar-identity` 信息卡，长用户名仅在卡片内安全省略，不再挤压 Logo 与关闭入口。
- 2026-08-19：修复文档管理加载失败时错误态、空表头与“暂无符合条件的文档”同时显示的回归；内容区改为错误态与列表/空态互斥渲染。
- 2026-08-19：入库任务的状态与任务类型字典从 `IngestionJobsView.vue` 收敛至 `ingestion-presentation.ts`，筛选选项与列表展示共用同一状态事实源；`ingestion` / `reindex` / `index_migration` 不再直接回显英文值。
- 2026-08-19：补齐审计中心“操作”列的中文事件映射，覆盖用户创建/更新/删除、角色、部门策略、文档索引与运行配置发布事件；未知动作使用中文兜底，不再直接显示英文事件码。
- 2026-08-19：Danger Button hover 改为与 Primary 相同的色阶机制：新增由 `--kb-color-danger` 与 surface 派生的 `--kb-color-danger-highlight`，映射官方 `--el-color-danger-light-3` / error light-3；filled Danger 恢复 Element 默认的“基色 → highlight、白字不变”交互，不再强制 hover 与默认态同色。三端浏览器回归校验 hover 变量命中 highlight token。
- 2026-08-19：修复 filled Danger Button hover 误用 `danger-soft` 浅背景并继续显示白字的问题；因现有 token 没有 danger-dark 色阶，hover / active 保持 `--kb-color-danger` 背景、边框与 `--kb-color-on-primary` 文字，确保对比度且不发明临时颜色。三端浏览器回归校验实际 CSS 变量映射。
- 2026-08-19：Primary Button 描边由偏重的 `--kb-color-primary-dark` 调整为柔和色阶：default 使用 `--kb-color-brand-highlight`，hover / active 使用 `--kb-color-primary`；继续保持边框与填充可辨，不改变尺寸和圆角。
- 2026-08-19：修复 filled Primary Button 因边框与背景同色而视觉膨胀的问题；保持 40px 高度、padding 与 md 圆角不变，仅将 default / hover / active 边框映射为 `--kb-color-primary-dark`，disabled 继续使用既有浅色阶。三端浏览器回归确认 Primary 边框存在且与背景色不同。
- 2026-08-19：按最终圆角层级将 `element.css` 内普通 Element 组件统一调整为 `--kb-radius-md`（10px），页面 `kb-block` 公共容器继续使用 `--kb-radius-lg`（12px）；pill、circle、Tag 与 bottom sheet 保留语义圆角。三端回归分别校验 Input/Button 为 md、工具栏容器为 lg。
- 2026-08-19：全站普通圆角统一提升到 `--kb-radius-lg`（12px），不修改或重复定义 token：`kb-block` 页面容器、Button、Input、Select、DatePicker、InputNumber、Pagination、Card、Dialog、Collapse 与 Upload 使用同一圆角；pill、circle、Tag 与 bottom sheet 保留语义圆角。三端 Playwright 同时校验工具栏、Input、Button 的计算圆角均等于 `--kb-radius-lg`，专项 1/1、Stylelint、ESLint、typecheck、生产构建与文档检查通过。
- 2026-08-19：按最新视觉要求撤销 Mobile `large` 控件策略，全站 Element Plus 由根级 ConfigProvider 固定为 `default`，并将 default / large / small 的 Button、Input、Select、DatePicker、InputNumber、Pagination、Tabs、Dropdown、Collapse、Upload 和弹层关闭按钮外框统一到 `--kb-control-height`。普通表单控件圆角统一为 `--kb-radius-sm`；Button 同步统一 padding、字号、图标 gap 和 circle 宽度。PC 1440px、Pad 768px、Mobile 375px 浏览器计算尺寸均确认 Input/Button 为 40/40；专项 Playwright 1/1、Stylelint、typecheck、生产构建通过。
- 2026-08-19：修复 Element Plus 2.14.3 默认 Button 未读取 `--el-component-size` 导致与 Input 高度不一致的问题；default Button 显式映射 `--kb-control-height`，large 映射 `--kb-control-height-touch`，并同步统一 padding、字号、圆角、图标 gap 与 circle 宽度。新增 PC 1440px、Pad 768px、Mobile 375px 浏览器计算尺寸断言，分别确认 Input/Button 为 40/40、40/40、44/44；专项 Playwright 1/1、Stylelint、typecheck、生产构建通过。
- 2026-08-19：基于本地确认的 Element Plus 2.14.3 重做 light theme：`element.css` 优先使用官方 `--el-*` 映射现有 `--kb-*` 色彩、背景、文本、边框、圆角、阴影、字号和尺寸 token，除 768/1280 既有媒体查询外无硬编码 `px`、颜色或业务 selector。根级 ConfigProvider 统一 Mobile `large`、Pad / PC `default`；Tabs、Dialog、Drawer、Collapse、Upload 等不支持统一交互 size 的组件成套覆盖高度、padding、字号与间距。问答 textarea 业务外观下沉至 `AskComposer.vue`，共享 Collapse 边框移至 `main.css`。新增尺寸 composable 单测 2/2，相关组件测试 12/12、根级 lint、Stylelint、typecheck、生产构建和三端 Playwright 2/3 通过；剩余 768px 用例在布局无横向溢出后被既有“问答历史”heading 语义断言阻塞。全量 Web 单测 86/88，仍仅为 Provider Anchor 两项既有断言。
- 2026-08-19：完成 Web CSS 三层职责重构：`tokens.css` 只声明颜色、字体、间距、控件与 768/1280 断点变量，根节点的字体、前景色与背景迁入 `main.css`；`element.css` 只保留跨页面通用的 Element Plus 原生类覆盖。Provider 配置表单、文档分块 Tabs、上传 Dialog 和来源 Drawer 的业务变体分别下沉至对应 Vue SFC scoped 样式；无挂载点的 `mobile-date-picker-popper` 被删除，其 Mobile 安全视口约束提升为全局 DatePicker 基线；用户目录与部门权限共同使用的 `kb-collapse-list` 保持单一全局变体。新增的样式归属规范已同步 AGENTS、前端设计、开发规范、任务清单与 Web README。根级 lint（含 Web ESLint、密钥扫描、文档检查）、Stylelint、typecheck、生产构建、相关组件测试 10/10 和三端按钮间距 Playwright 1/1 通过；全量 Web 单测 84/86，仍仅为 Provider Anchor 的两项既有断言。另 5 项相关 Playwright 分别在既有标题语义、用户页动态导入、Provider 标题、移动用户筛选 placeholder 与上传 metadata 空格断言处提前阻塞，未将其记为样式回归通过。
- 2026-08-19：优化文档分块卡片布局，将标题与位置、Chunk ID、metadata、原始/脱敏正文分层；metadata 复用 `kb-data-fields` 与无分隔线 modifier，相邻 Chunk ID 按上一个/下一个分行并允许 fingerprint 安全换行；Desktop/Pad 保持正文双栏，Mobile 改用 Element Plus Tabs 且一次只挂载一份正文。分块专项单测 2/2、Prettier、Stylelint、目标 ESLint、typecheck、生产构建通过；既有分块 Playwright 仍在进入目标页前被“用户与角色”页面标题断言阻塞，未将其记为通过。
- 2026-08-19：完成 Vue CSS 二次精简与 classname 收口：删除空 style/媒体查询/规则、重复公共声明、无效 Grid/Flex 属性和无用途 class；`main.css` 重排并抽取双栏、状态工具栏和两级错误态，状态命名统一为 `is-*`，Element 覆盖继续集中在 `element.css`。Stylelint、ESLint、typecheck、生产构建通过；全量 Web 单测 82/84，仍仅为 Provider Anchor 两项既有断言；CSS 专项 Playwright 4/5，剩余用户目录用例停在既有移动管理员筛选断言，补跑的用户目录专项又被 `/kb-block/` 同时命中 `kb-block-scroll` 的既有断言阻塞。
- 2026-08-19：问答历史三端均移除开始/结束时间与对应 URL/API 筛选状态；PC 与 Pad 的 `history-toolbar` 保持单行“会话搜索 + 筛选/重置”，Mobile 移除筛选 Drawer 并将“筛选 / 重置”直接放在搜索框右侧。历史专项单测 5/5、typecheck、目标 ESLint、Stylelint 和 PC/Pad/Mobile Playwright 1/1 通过。
- 2026-08-19：问答历史列表移除 Element Plus 页码分页和 URL 页码状态，改为上划到列表底部附近时按 `offset/limit` 追加下一批；补齐并发请求锁、追加去重、筛选竞态隔离、增量失败重试和全部加载提示。历史专项单测 4/4、typecheck、目标 ESLint、Stylelint、生产构建和 Playwright 1/1 通过；全量 Web 单测 80/82，仍仅被 Provider Anchor 的两项既有断言阻塞。
- 2026-08-19：完成全量 Web classname 正反向审计；移除无 CSS、无脚本行为、无必要测试用途的模板 class，删除无节点命中的孤立选择器，并将原依赖业务内容 class 的 E2E 定位改为共享容器结构。全量 Web Prettier、ESLint、Stylelint、typecheck、13 项受影响组件测试、文档检查、生产构建及跨页面容器/移动返回入口专项 Playwright 2/2 通过。
- 2026-08-19：全站内容容器的滚动职责收敛为 `kb-page__content`（整页内容滚动）与 `kb-block-content` / `kb-block-scroll`（固定外壳 + 内部滚动）两种模型；系统状态移除互相覆盖的双重 class，入库任务、文档分块和用户目录删除重复的 Flex/overflow 声明，Mobile padding 改为显式 modifier。Web typecheck、目标 ESLint、Stylelint、受影响组件测试 5/5、文档检查、生产构建和跨页面 375px 专项 Playwright 1/1 通过。全量 Web 单测 79/81，仍被 Provider Anchor 的两项既有断言阻塞；补跑的三项历史响应式用例中，用户目录已通过新的内部滚动检查，另外两项先被既有移动/管理页标题断言阻塞，横屏综合用例继续在既有部门外层滚动断言处失败，未将其记为全绿。
- 2026-08-19：文档管理与审计中心的 Mobile 内容面板收敛为同一 `kb-block` 结构，统一外壳、内容滚动、间距、空态和分页视觉。Web typecheck、目标 ESLint、Stylelint、5 项组件测试、文档检查、生产构建和 375px 专项 Playwright 1/1 通过。
- 2026-08-19：Pad 图标 sidebar 的 hover、键盘聚焦与触屏长按浮层收敛为只显示当前入口，不再展开同组的全部入口。Web typecheck、目标 ESLint、文档检查、生产构建和 900px 专项 Playwright 1/1 通过。
- 2026-08-19：`useBreakpoint()` 删除与 `isMobile` 查询条件完全相同的 `isPhone` 别名；Vue 结构分支统一使用 `isMobile`（`max-width: 767px`）。
- 2026-08-18：问答工作台顶部移除页面私有 `ask-header` / `eyebrow`，复用其他菜单的 `kb-page-header`、`kb-title-group` 和 `kb-action-group` 结构；标题基础视觉从 AppShell scoped 样式抽至 `main.css`，不再有两套页面标题布局。
- 2026-08-18：页面一级标题由无命名空间的 `heading heading--h1` 统一迁为 `kb-heading--h1`；基础字号在 `main.css` 集中定义，页面只保留自身的上下文尺寸差异。
- 2026-08-18：所有块级容器、modifier、列表和标题结构统一为单一 `kb-block*` 命名，删除并不再兼容 `kb-card`、`kb-panel`、`kb-card--*`、`kb-panel--*` 与 `kb-card__*` class；所有基础 padding 与 Element Card 统一为 `--kb-block-padding`，移除重复的 Card/Panel token 别名。原 `text-block` 与无效的后代 warning 选择器统一为 `kb-text` / `kb-text--warning`，状态样式直接挂在内容节点。Mobile 用户目录的 `access-table-wrap` 填满工具栏与权限说明之间的可用空间，并在自身滚动。所有 Web 样式文件按“紧凑 / Pad / Mobile / 低高度 Mobile 横屏 / 交互能力”顺序整理媒体查询、补齐统一注释，并清理空媒体查询；`main.css` 依重置、全局基础、页面布局、块容器、公共结构、反馈、品牌、回答来源、分页和无障碍顺序组织。
- 2026-08-18：按当前页面规范将用户目录“筛选部门”改为由受控部门目录驱动的 `el-select`，PC/Pad 与 Mobile 筛选 Drawer 一致；用户目录与部门权限的手机折叠列表收敛为 `kb-collapse-list` / `kb-collapse-list__summary`。新增 `kb-panel` 作为结构化列表、表格和工具栏的块级基础容器，与 `kb-card` 共同集中基础视觉和 modifier；删除页面内重复的筛选触发、移动折叠列表、Panel 头部及无引用样式。Web typecheck、ESLint、Stylelint、文档检查、生产构建、81 项单元测试及用户目录专项 Playwright 均通过。
- 2026-08-17：统一审计、用量、用户目录、文档详情与 Provider 摘要的数据卡片为 `kb-data-*` 语义结构，由 `kb-card` 提供基础视觉；删除 `mobile-data-*` / `mobile-field-*` 与 Provider 的重复字段行样式。Web 单测 81/81、Stylelint、ESLint、typecheck、Provider 专项 Playwright、生产构建与文档检查通过。
- 2026-08-17：用量与入库任务工具栏在 Pad（768–1279px）恢复单行布局；Mobile 继续使用既有筛选 Drawer 与重排。两项专项 Playwright 回归、Web ESLint、typecheck、生产构建与文档检查通过。
- 2026-08-17：修复审计中心游标分页的总数回归；Element Plus 分页重新绑定 API 返回的 tenant/type 范围 `total`，避免用可见游标页数推算记录总数。新增审计页分页回归测试；Web 全量单测 80/80、ESLint 与 typecheck 通过。
- 2026-08-17：完成 Web 端 UI/UX 与响应式样式收口。Design Tokens 扩展为语义 surface、状态背景、控件尺寸、字号、阴影与过渡；Element Plus 的按钮、输入、表格、Tabs、分页、Dialog 与 Drawer 统一为同一视觉与移动可用性基线。将跨页面错误态、移动筛选与数据卡片抽至 `main.css`，清理各页重复声明和无效注释，并移除 900px/901px 的局部样式分叉，统一沿现有 768px/1280px 断点重排。Web ESLint、typecheck、79 项单元测试与生产构建通过；完整 Playwright 回归仍在首个既有导航断言处失败（从问答页跳转历史页未发生），本轮未改动路由或导航逻辑。
- 2026-08-17：进一步统一 Web 基础规格：Element Plus 的输入、选择、日期、数字与按钮控件在 PC / Pad / Mobile 均使用 40px 高度；移除手机端按钮单独升至 44px 的分叉。新增 `kb-card` 与 compact / spacious / flush / interactive modifier，将 Provider、详情、预览、用量、系统、历史、任务与数据容器的重复 Card 基础样式收敛到全局 Token，不改变各页面业务布局或行为。
- 2026-08-17：移除业务模板中的通用 `panel` class，以 `kb-card` 作为当时唯一基础 Card class。Provider 数据卡、文档详情 `detail-grid`、工具栏、来源、数据表容器和各类同层级卡片均以“业务语义 class + `kb-card`（及 modifier）”表达；基础视觉不再由业务 class 分散定义。该阶段性约定已由 2026-08-18 的 `kb-panel` 容器规范补充。
- 2026-08-10：同源 PDF/Office 预览的 iframe 允许策略与跨域嵌入防护已落地。
- 2026-08-11：问答上下文联动、行内来源和历史来源详情已完成安全回归。
- 2026-08-11：完成第二轮全量 Markdown 审查，修正文档与运行时事实漂移，并将格式、链接、端点、Provider 与断点检查纳入 `pnpm docs:check` 和 CI。
- 2026-08-11：阶段 4/7 未完成项从“已完成阶段”折叠区移出，明确为不阻塞阶段 16 的延期增强与优化；Embedding 遥测按现有实现更正为已完成。
- 2026-08-11：完成 DOC 经内网 Tika 解析和本地 PDF 预览、Google `gemini-embedding-001` 运行时接入、完整指纹文本哈希向量缓存，以及失败后跳过已缓存批次的 Embedding checkpoint 续跑。
- 2026-08-11：用量页在零查询时展示当前 Embedding Provider/model，并明确其数量是“涉及问答”而非供应商请求；审计中心的知识问答只展示实际 LLM，云端策略展示并固化当时的 Embedding Provider/model，历史事件按 collection 事实回填已支持模型。
- 2026-08-11：阶段 14 使用 44 条真实问题完成 DeepSeek 双策略质量评测；越权泄露率与错误率均为 0，成本覆盖率 100%，本地 BGE 候选未带来 Recall 增益且显著增加延迟/成本，因此保持 Rerank 默认关闭。评测同时补齐 DeepSeek 缓存差异计价、合并来源的稳定 chunk 匹配和本地 cross-encoder 512-token 推理边界。
- 2026-08-11：修复邻块跨 PDF 页/工作表合并导致来源位置继承首块的问题；同页连续块仍按预算合并，页码或工作表变化时强制切组，确保问答来源打开到实际内容所在页。
- 2026-08-11：补齐租户级用量事实：问答请求作用域采集实际 Provider telemetry，并与查询审计原子持久化输入/输出 token 和调用时估算成本；用量页按 tenant/时间聚合，迁移前历史、Provider 未回传 usage 或未配置价格时继续保持未知，页面缺失值统一展示“暂无数据”。
- 2026-08-13：完成前端 CSS 归属整理：`main.css` 收敛到 reset、全局无障碍/动效、回答来源/Markdown 与 Element Plus Teleport 浮层规则；审计、用户、历史、部门、文档、入库、用量、Provider、系统状态和错误页等页面/业务组件样式分别迁入对应 Vue SFC，并先保留必要的页面间重复声明，公共 CSS 后续再抽象。已通过 Web CSS lint、ESLint、typecheck、77 项单元测试和生产构建。全量 `phase15.spec.ts` 为 15/24；其余 9 项仍涉及当前导航/历史/移动详情/任务与部门/更多抽屉/上传拖放区断言及开发服务器动态导入失败，未把本轮结果记为 E2E 全绿。
- 2026-08-13：前端 UX/UI 响应式基线更新为 PC（不可折叠的展开四组侧栏）/ Pad（全部入口图标、组间分隔线、顶部面包屑及 hover、键盘或长按浮层菜单）/ Mobile（三个高频入口 +“更多”底部导航，以及带身份头部、分组卡片和安全退出的底部抽屉）。设计 token 更新为低饱和靛蓝和中性灰；文档管理在 Pad 以下使用卡片列表。已通过 Web typecheck、ESLint、74 项单元测试和生产构建。
- 2026-08-13：问答历史按参考稿完成 PC / Pad / Mobile 三档视觉与交互对齐：桌面统一 32px 单行筛选并将列表、分页和详情收进一体式卡片，Pad 保持窄列表双栏并将筛选拆成两行，Mobile 使用顶部菜单页头、搜索 + 文字筛选入口、列表/详情两级全宽卡片、左滑删除与 Element Plus 页码分页；专项 Playwright 回归此前为 2/2、Web 单测 74/74、typecheck、ESLint、Stylelint、文档检查和生产构建通过。本次分页组件调整按用户要求未运行 `phase15.spec.ts`。完整 Playwright 为 15/19，仍有入库步骤、部门横屏滚动、“更多”抽屉卡片背景和管理页壳层对齐 4 项现有响应式用例待当前 UX/UI 重构收口。
- 2026-08-13：文档管理工具栏对齐问答历史的三端布局；上传文档弹层按参考稿重做为 PC 460px / Pad 400px 居中 Dialog 与 Mobile 全屏底部卡片，手机正文独立滚动且取消/开始上传等宽吸底。文件队列统一为上传中、`failed` 和 `queued` 卡片，上传请求使用同源带凭据 XHR 展示真实字节进度，响应仍执行运行时 schema 验证和统一安全错误映射。已通过文档专项 Playwright 4/4、Web 单测 77/77、typecheck、ESLint、Stylelint、文档检查和生产构建。
- 2026-08-13：移动端问答与历史的来源详情改为复用“更多”视觉语言的底部弹层，使用全视口遮罩、顶部拖拽把手和右上角关闭按钮；PC / Pad 继续使用右侧抽屉，打开详情不切换路由。已通过来源组件测试 4/4、Web 单测 75/75、typecheck、ESLint、Stylelint、文档检查和生产构建；按本轮要求未运行或修改 `phase15.spec.ts`。
- 2026-08-13：文档详情按参考稿完成三档卡片布局：PC 双摘要卡并排，Pad 堆叠，Mobile 仅保留右上角“⋯”并以底部操作面板承载预览、修改权限、重新索引和红色删除项；DWG 转换 warning 展示为蓝色格式转换说明。文档详情专项 Playwright 1/1、Web 单测 77/77、目标 ESLint/Stylelint、文档检查和 Vite 生产打包通过；全量 Web typecheck 仍被 `DocumentsView.vue` 与 `HistoryView.vue` 既有的三处 `type="button"` 类型错误阻塞，本页未新增类型错误。
- 2026-08-13：入库任务筛选栏对齐问答历史的 PC / Pad / Mobile 三端布局：桌面单行、Pad 两行，Mobile 保留文档 ID 输入与文字“筛选”入口，状态收入底部 Drawer。已通过专项 Playwright 1/1、Web 单测 77/77、typecheck、ESLint、Stylelint、文档检查和生产构建。
- 2026-08-13：`main.css` 中入库卡片与工具栏专属样式迁入 `IngestionJobsView.vue` scoped 样式，删除无使用的 `dl/dt/dd`、旧筛选表单和重复响应式规则；任务内容面板通过 `overflow: hidden` 使圆角实际裁剪内部滚动区与分页。已通过专项 Playwright 1/1、Web 单测 77/77、typecheck、ESLint、Stylelint 和生产构建。
- 2026-08-13：入库任务列表改为按状态默认展开：进行中和失败任务展开，完成及其他终态收起；收起行仅保留摘要、7 点进度、状态和展开指示，完整步骤与清单只在展开时挂载。Pad 展开步骤改为块内横向滚动，Mobile 保留垂直步骤。已通过入库任务专项 Playwright 2/2、Web 单测 77/77、typecheck、ESLint、Stylelint、文档检查和生产构建。
- 2026-08-13：修复入库任务 Mobile 长文件名撑宽列表并产生水平偏移，任务容器及摘要链路强制 `min-width: 0`、文件名省略且列表只允许 Y 轴滚动；Pad 步骤横向滚动背景迁到滚动视口，内容背景改为透明，消除滚动后白底缺口。已通过入库任务专项 Playwright 2/2、Web 单测 77/77、typecheck、ESLint、Stylelint、文档检查和生产构建。
- 2026-08-13：修复模型 Provider 页在应用壳层内无法纵向滚动；补回页面级 Flex 高度约束，使长表单继续由 `.kb-page__content` 独立滚动，并增加实际 `scrollTop` 回归断言。已通过专项 Playwright 1/1、Web 单测 77/77、typecheck、目标 ESLint/Stylelint、文档检查和生产构建。
- 2026-08-14：前端业务 CSS 选择器统一改为“父容器语义 + 元素用途”的 class，保留 `strong`、`span`、`article`、`header`、表格和列表等原生标签；Markdown 动态节点及相关脚本/回归定位同步使用 class。仓库复扫只保留 `main.css` 的全局 reset/base 标签选择器与受控第三方样式例外。Web Stylelint、ESLint、typecheck、78 项单元测试、文档检查和生产构建通过；完整 Playwright 为 17/24，其中问答 Markdown/来源、900px 分组导航和核心问答页 WCAG AA 通过，另 7 项仍为历史卡片/返回按钮、移动详情操作、任务步骤、部门横屏滚动、更多抽屉背景和上传拖放区高度等现有布局断言，未将本轮记为 E2E 全绿。
- 2026-08-14：移动端应用壳层统一隐藏 `.kb-title-group`，不再只对问答历史做路由特判；无返回操作时同时移除空的 `kb-page-header`，文档详情等来源页面只保留返回入口。Web typecheck、ESLint、Stylelint 和移动端页头专项 Playwright 1/1 通过。
- 2026-08-14：修复 `SafeMarkdown` 样式迁入 scoped SFC 后无法命中 `v-html` 节点的回归；表格恢复原生 table 布局、左/中/右对齐和可聚焦的块内横向滚动，并补齐 1–6 级标题、非 1 起始有序列表、分隔线、换行、粗体、斜体和删除线回归。Web 78 项单测、typecheck、ESLint、Stylelint 与 Markdown 专项 Playwright 1/1 通过。
- 2026-08-14：Element Plus 原生类覆盖从业务 SFC 与 `main.css` 集中迁入全局 `styles/element.css`，入口只引入一次；表格固定表头、移动筛选控件、权限折叠面板等通用规则直接使用 `.el-*`，未为迁移新增共享业务 class。Drawer、Dialog、日期浮层等确有互斥样式的场景只保留组件自身 class 或 popper-class。仓库复扫确认业务 Vue/CSS 不再包含 `.el-*` 覆盖；Web Stylelint、ESLint、typecheck、78 项单元测试、文档检查和生产构建通过。完整 Playwright 结果与迁移回调前一致为 16/26，剩余 10 项仍是 Markdown fixture、历史卡片/返回入口、分块标题、移动详情/任务/部门、上传区和更多抽屉等现有断言，未将本轮记为 E2E 全绿。
- 2026-08-14：全站 `el-drawer` 统一为同一视觉壳层：header/body/footer、标题与关闭按钮、分隔线、背景、阴影和安全区由 `element.css` 的原生类规则集中提供，底部 Drawer 统一 18px 顶部圆角与拖拽把手；来源详情、更多导航和文档操作删除各自手写的把手及标题栏，只保留方向、容量和正文布局差异。修复“更多”Drawer 重复关闭按钮并统一导航卡片背景；来源、历史筛选、上传和更多 Drawer 专项 Playwright 4/4 通过。
- 2026-08-14：文档操作 Drawer 的 `mobile-action-list` 对齐“更多”Drawer：操作项统一为图标、文字、右侧箭头三列卡片，复用背景、边框、圆角与 hover 反馈，危险操作保留红色语义；“更多”Drawer 的文本关闭符号改为与其他 Drawer 相同尺寸的 Element `Close` 图标。Web Stylelint、目标 ESLint、typecheck、79 项单元测试、文档检查和生产构建通过。
- 2026-08-14：修复问答历史与用量页 DatePicker 被 Element 默认宽度覆盖的问题；`element.css` 通过原生 `.el-date-editor` 组合选择器统一填满可收缩列，用量筛选从 Flex 收敛为两个等宽日期列加操作按钮的 Grid。历史 Mobile Drawer 与用量桌面专项 Playwright 2/2 通过。
- 2026-08-14：按最终确认调整问答历史 Pad 筛选栏：第二行由两个等宽弹性日期列和固定操作列组成，开始/结束时间撑满操作组之外的全部剩余宽度，结束时间与操作按钮之间只保留标准 gap，不设置额外空白列。针对 Element DatePicker 组件样式延迟加载后恢复默认 220px 的情况，全局原生组合选择器使用明确优先级覆盖；768px 专项 Playwright 已验证实际宽度大于 220px 且无额外间隙。
- 2026-08-14：移动端问答历史详情的“← 返回会话列表”入口已移入应用壳层 `kb-action-group`，与“返回文档列表”、“返回文档详情”统一位置和视觉规格；历史详情卡顶部只保留会话标题，返回时仅清理 URL 的 `conversationId`并保留原筛选与页码。目标 ESLint、Stylelint、Web typecheck、79 项单元测试、生产构建、文档检查与历史页专项 Playwright 1/1 通过。
- 2026-08-14：用户目录与用量工具栏对齐文档管理的 PC / Pad / Mobile 三端节奏，删除工具栏内重复页面标题和长说明；必要的“涉及问答”指标口径移到 Provider 数据卡内。用户目录在 Mobile 保留搜索、新增账号和筛选入口，Drawer 只承载部门；用量日期在 Mobile 全部收入筛选 Drawer。目标 ESLint、Stylelint、Web typecheck、79 项单元测试、生产构建、文档检查与三端专项/管理页 Playwright 回归 2/2 通过。
- 2026-08-14：移动端应用菜单由底部“问答 / 文档 / 审计 / 更多”导航改为顶部汉堡按钮控制的左侧 sidebar；侧栏复用 PC 的四组入口、当前项状态与 capability 过滤，保留身份摘要和安全退出，页面正文不再为底部菜单预留 68px。Web typecheck、ESLint、Stylelint、79 项单元测试、文档检查、生产构建和移动 sidebar 专项 Playwright 1/1 通过；补跑的历史三端响应式用例通过，另两个综合用例仍分别受已有的文档详情动态导入/移动操作区和部门横屏滚动断言阻塞，未将全量 E2E 记为全绿。
- 2026-08-14：修复 Element Plus 相邻按钮间距叠加：全局 `.el-button + .el-button` 左外边距归零，PC/Pad/Mobile 工具栏、移动筛选、表格行操作、Dialog/Drawer footer 与 CAD 缩放按钮组均改由父容器显式 `gap` 控制。仓库样式扫描只保留归零规则；Web ESLint、Stylelint、typecheck、79 项单元测试、文档检查、生产构建与三端按钮隔离 Playwright 1/1 通过，文档/用户/用量工具栏及 Pad 上传关联用例 3/3、入库三端和历史响应式用例各 1/1 通过。另两个上传综合用例仍先被已有的移动文件选择区高度和桌面拖放区高度断言阻塞，未将全量 E2E 记为全绿。

## 6. 交付要求

完成本阶段前必须更新本文、[开发任务清单](./docs/05-开发任务清单.md) 和受影响的规格/技术/运维文档，并如实记录实际验证结果与未完成风险。
